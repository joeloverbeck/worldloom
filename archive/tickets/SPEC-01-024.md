# SPEC-01-024: Retune `named_entity` extraction against hyphen-split heading fragments

**Status**: COMPLETED
**Priority**: MEDIUM
**Effort**: Medium
**Engine Changes**: Yes — `tools/world-index` stoplist coverage and rebuilt-`animalia` regression coverage for hyphen-split heading fragments that still persist as `named_entity` rows.
**Deps**: `archive/tickets/SPEC-01-021.md`, `archive/tickets/SPEC-01-022.md`, `archive/tickets/SPEC-01-023.md`, `archive/specs/SPEC-01-world-index.md`, `docs/FOUNDATIONS.md`

## Problem

Fresh rebuild audit on `2026-04-22` shows `worlds/animalia/_index/world.db` is structurally clean but still semantically noisy on one narrow `named_entity` seam. After:

1. `cd tools/world-index && npm run build`
2. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
3. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`

the rebuilt DB still persists hyphen-split heading/reference fragments as first-class entities:

- `Age Linguistic Recovery` (`5` mentions)
- `Career Contractor Pension` (`4` mentions)
- `Both Subsection` (`2` mentions)
- `Attempt Sub` (`1` mention)
- `Candidate Log` (`1` mention)
- `Captain Cycles` (`1` mention)

These are not stale checked-in `_index` residue. They reproduce after fresh rebuild, and the source rows are document-heading fragments or suffixes of larger hyphenated labels rather than intended world entities. That weakens the machine-facing semantic surface that downstream retrieval and audit consumers would query directly from SQLite.

## Assumption Reassessment (2026-04-22)

1. The rebuilt index is structurally trustworthy. Fresh `animalia` rebuild + `verify` succeeded, `validation_results` is empty, unresolved edges are `0`, dangling edge sources/targets are `0`, dangling `entity_mentions` are `0`, `PRAGMA integrity_check` is `ok`, and `file_versions` covers `59` files, matching the live enumerator contract in `tools/world-index/src/enumerate.ts` after excluding intentional `INDEX.md` omissions.
2. `tools/world-index/src/commands/shared.ts` still includes broad prose surfaces such as `section`, `subsection`, `bullet_cluster`, `proposal_card`, `proposal_batch`, `character_record`, and `character_proposal_batch` in `ENTITY_SOURCE_NODE_TYPES`, so inline references to heading labels and whole-file prose remain heuristic extraction input by design.
3. `tools/world-index/src/parse/entities.ts` still applies `CAPITALIZED_MULTIWORD_REGEX` over those prose-node bodies. The current survivors show that earlier stoplist waves removed direct workflow/document labels, but suffix fragments from larger hyphenated labels still leak through as standalone `named_entity` rows.
4. Shared boundary under audit: heuristic candidate collection/filtering in `tools/world-index/src/parse/entities.ts`, stoplist/fragment suppression in `tools/world-index/src/parse/stoplist.ts`, the persisted `nodes` / `entity_mentions` rows in SQLite, and downstream retrieval/audit consumers that treat `named_entity` rows as semantic entities rather than document furniture.
5. Exact rebuilt DB evidence from `worlds/animalia/_index/world.db`:
   - `Canonical name: Age Linguistic Recovery | Kind: unknown | Mentions: 5`
   - `Canonical name: Career Contractor Pension | Kind: unknown | Mentions: 4`
   - `Canonical name: Both Subsection | Kind: unknown | Mentions: 2`
   - `Canonical name: Attempt Sub | Kind: unknown | Mentions: 1`
   - `Canonical name: Candidate Log | Kind: unknown | Mentions: 1`
   - `Canonical name: Captain Cycles | Kind: unknown | Mentions: 1`
6. Exact source examples confirm these are heading/reference fragments, not intended entities:
   - [worlds/animalia/OPEN_QUESTIONS.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/OPEN_QUESTIONS.md:85) defines `## Maker-Age Linguistic Recovery`, and adjudication prose in [worlds/animalia/adjudications/PA-0001-accept_with_required_updates.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/adjudications/PA-0001-accept_with_required_updates.md:496) and [worlds/animalia/adjudications/PA-0003-accept_with_required_updates.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/adjudications/PA-0003-accept_with_required_updates.md:504) reproduces the truncated survivor `Age Linguistic Recovery`.
   - [worlds/animalia/OPEN_QUESTIONS.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/OPEN_QUESTIONS.md:157) defines `## Post-Career Contractor Pension and Stigma`, and prose in [worlds/animalia/proposals/PR-0002-heritable-contamination-registry.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/proposals/PR-0002-heritable-contamination-registry.md:143) and [worlds/animalia/adjudications/PA-0014-accept_with_required_updates.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/adjudications/PA-0014-accept_with_required_updates.md:168) yields the suffix fragment `Career Contractor Pension`.
   - [worlds/animalia/TIMELINE.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/TIMELINE.md:108) defines `Crafter-Attempt Sub-Stream`, which reproduces as `Attempt Sub` in [worlds/animalia/characters/melissa-threadscar.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/characters/melissa-threadscar.md:202).
   - [worlds/animalia/character-proposals/batches/NCB-0001.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/character-proposals/batches/NCB-0001.md:150) defines `## Phase 12 Rejected-Candidate Log`, which reproduces as `Candidate Log`.
   - [worlds/animalia/INSTITUTIONS.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/INSTITUTIONS.md:43) contains `Both Subsection-Seal Fraud Precedent`, which reproduces as `Both Subsection`.
   - [worlds/animalia/OPEN_QUESTIONS.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/OPEN_QUESTIONS.md:305) defines `## Bardic Register for Mutated-Beast Cycles vs Bandit-Captain Cycles`, which reproduces as `Captain Cycles`.
7. `archive/tickets/SPEC-01-021.md`, `archive/tickets/SPEC-01-022.md`, and `archive/tickets/SPEC-01-023.md` already removed earlier waves of workflow labels, checkpoint prose, inline document-reference labels, and simpler phrase fragments. The current survivors are a narrower adjacent seam: suffix fragments carved out of larger hyphenated headings or compounds that still leak through after those fixes.
8. No active ticket owns this seam. `tickets/` contained no existing `SPEC-01-*` ticket for `Age Linguistic Recovery`, `Career Contractor Pension`, `Both Subsection`, `Attempt Sub`, `Candidate Log`, or `Captain Cycles` at audit time.
9. `docs/FOUNDATIONS.md` requires canon facts to reinforce the world model rather than turning document-heading fragments into ontology-like entities. These persisted rows weaken that contract instead of clarifying it.
10. Mismatch + correction: this seam is semantic, not structural. The truthful fix is another bounded canonical-stoplist retune rather than schema, rebuild-path, or extractor-algorithm work.
11. Mismatch + correction: targeted extractor probing during implementation showed the reproduced survivors are all already-captured candidate shapes. Extending `tools/world-index/src/parse/stoplist.ts` was sufficient; no `tools/world-index/src/parse/entities.ts` algorithm change was required for this bounded seam.
12. Mismatch + correction: `Canon Althea` / `Canon Althea Greystone` remain adjacent but are not included here because the current audit evidence does not yet separate titled-person retrieval policy from pure fragment noise cleanly enough. This ticket is intentionally limited to unambiguous fragment captures.

## Architecture Check

1. The clean fix is to keep the canonical extractor shape unchanged and extend the canonical stoplist with the reproduced hyphen-split suffix fragments. That is cleaner than adding a second downstream semantic blacklist or broadening the extractor into a riskier algorithm rewrite for a bounded audited seam.
2. No backwards-compatibility aliasing or parallel "filtered entity" layer should be introduced. The canonical `named_entity` surface itself should become trustworthy.

## Verification Layers

1. Hyphen-split heading/reference fragments no longer persist as `named_entity` rows after a fresh `animalia` rebuild. -> readonly DB query for `Age Linguistic Recovery`, `Career Contractor Pension`, `Both Subsection`, `Attempt Sub`, `Candidate Log`, and `Captain Cycles`
2. Legitimate retained entities remain present after the retune. -> readonly DB query for `Vespera Nightwhisper`, `Melissa Threadscar`, `Copper Weir`, and `Charter Hall`
3. The fix remains semantic-only; rebuilt `animalia` stays structurally clean. -> `node tools/world-index/dist/src/cli.js build animalia`, `node tools/world-index/dist/src/cli.js verify animalia`, plus readonly unresolved/dangling queries
4. The bounded fragment class is blocked without regressing earlier stoplist/document-reference fixes. -> targeted extractor/unit coverage on `tools/world-index`

## What to Change

### 1. Retune the extractor boundary for hyphen-split suffix fragments

Add the minimum canonical stoplist entries needed so suffix fragments carved out of larger hyphenated headings or compounds stop becoming standalone entities.

### 2. Add rebuilt-`animalia` regression coverage

Encode the audited false positives directly in package tests so future rebuilds cannot silently repollute the semantic surface with this fragment class.

## Files to Touch

- `tools/world-index/src/parse/stoplist.ts` (modify)
- `tools/world-index/tests/entities.test.ts` (modify)
- `tools/world-index/tests/integration/build-animalia.test.ts` (modify)

## Out of Scope

- Modifying `worlds/animalia/` content to accommodate the extractor
- Structural schema changes unrelated to `named_entity` extraction
- Reopening already-fixed `SPEC-01-021` / `SPEC-01-022` / `SPEC-01-023` examples that no longer reproduce after rebuild
- The policy question around titled-person rows such as `Canon Althea` where the current audit evidence does not yet cleanly separate fragment noise from deliberate retrieval targets

## Acceptance Criteria

### Tests That Must Pass

1. A rebuilt `animalia` index contains `0` `named_entity` rows for `Age Linguistic Recovery`, `Career Contractor Pension`, `Both Subsection`, `Attempt Sub`, `Candidate Log`, and `Captain Cycles`.
2. A rebuilt `animalia` index still contains legitimate retained entities including `Vespera Nightwhisper`, `Melissa Threadscar`, `Copper Weir`, and `Charter Hall`.
3. `cd tools/world-index && npm run build`
4. `cd tools/world-index && node --test dist/tests/entities.test.js`
5. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
6. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
7. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`

### Invariants

1. `named_entity` rows represent world entities or deliberate retrieval targets, not suffix fragments of larger hyphenated headings or document-reference labels.
2. Semantic cleanup must not require downstream consumers to subtract fragment noise from the canonical entity surface.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/entities.test.ts` — covers hyphen-split heading/reference fragments at the heuristic boundary.
2. `tools/world-index/tests/integration/build-animalia.test.ts` — proves the rebuilt live corpus no longer emits the audited fragment rows while retaining known-good entities.

### Commands

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
5. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`
6. `cd tools/world-index && node -e "const Database=require('better-sqlite3'); const db=new Database('../../worlds/animalia/_index/world.db',{readonly:true}); const banned=['Age Linguistic Recovery','Career Contractor Pension','Both Subsection','Attempt Sub','Candidate Log','Captain Cycles']; const kept=['Vespera Nightwhisper','Melissa Threadscar','Copper Weir','Charter Hall']; const count=(sql,...args)=>db.prepare(sql).get(...args).c; console.log(JSON.stringify({ banned: banned.map(name => ({name,count: count(\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug='animalia' AND node_type='named_entity' AND body LIKE ?\", 'Canonical name: '+name+' |%')})), kept: kept.map(name => ({name,count: count(\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug='animalia' AND node_type='named_entity' AND body LIKE ?\", 'Canonical name: '+name+' |%')})), validation_results: count(\"SELECT COUNT(*) AS c FROM validation_results WHERE world_slug='animalia'\"), unresolved_edges: count(\"SELECT COUNT(*) AS c FROM edges WHERE target_unresolved_ref IS NOT NULL\"), dangling_entity_mentions: count(\"SELECT COUNT(*) AS c FROM entity_mentions em LEFT JOIN nodes n ON n.node_id = em.node_id WHERE n.node_id IS NULL\"), dangling_edge_sources: count(\"SELECT COUNT(*) AS c FROM edges e LEFT JOIN nodes n ON n.node_id = e.source_node_id WHERE n.node_id IS NULL\"), dangling_edge_targets: count(\"SELECT COUNT(*) AS c FROM edges e LEFT JOIN nodes n ON n.node_id = e.target_node_id WHERE e.target_node_id IS NOT NULL AND n.node_id IS NULL\")}, null, 2)); db.close();"`

## Outcome

- Completion date: 2026-04-22.
- Extended `tools/world-index/src/parse/stoplist.ts` with the six audited hyphen-split heading/reference suffix fragments: `Age Linguistic Recovery`, `Career Contractor Pension`, `Both Subsection`, `Attempt Sub`, `Candidate Log`, and `Captain Cycles`.
- Added unit coverage that exercises those fragment shapes directly and expanded rebuilt-`animalia` integration coverage so the audited rows stay absent while known-good entities remain queryable.
- Kept the fix inside the existing extractor boundary; no `entities.ts` algorithm change or source-node contraction was required for this ticket.

## Verification Result

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
5. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`
6. `cd tools/world-index && node -e "const Database=require('better-sqlite3'); const db=new Database('../../worlds/animalia/_index/world.db',{readonly:true}); const banned=['Age Linguistic Recovery','Career Contractor Pension','Both Subsection','Attempt Sub','Candidate Log','Captain Cycles']; const kept=['Vespera Nightwhisper','Melissa Threadscar','Copper Weir','Charter Hall']; const count=(sql,...args)=>db.prepare(sql).get(...args).c; console.log(JSON.stringify({ banned: banned.map(name => ({name,count: count(\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug='animalia' AND node_type='named_entity' AND body LIKE ?\", 'Canonical name: '+name+' |%')})), kept: kept.map(name => ({name,count: count(\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug='animalia' AND node_type='named_entity' AND body LIKE ?\", 'Canonical name: '+name+' |%')})), validation_results: count(\"SELECT COUNT(*) AS c FROM validation_results WHERE world_slug='animalia'\"), unresolved_edges: count(\"SELECT COUNT(*) AS c FROM edges WHERE target_unresolved_ref IS NOT NULL\"), dangling_entity_mentions: count(\"SELECT COUNT(*) AS c FROM entity_mentions em LEFT JOIN nodes n ON n.node_id = em.node_id WHERE n.node_id IS NULL\"), dangling_edge_sources: count(\"SELECT COUNT(*) AS c FROM edges e LEFT JOIN nodes n ON n.node_id = e.source_node_id WHERE n.node_id IS NULL\"), dangling_edge_targets: count(\"SELECT COUNT(*) AS c FROM edges e LEFT JOIN nodes n ON n.node_id = e.target_node_id WHERE e.target_node_id IS NOT NULL AND n.node_id IS NULL\")}, null, 2)); db.close();"`

## Deviations

- The draft expected an extractor-code change in `tools/world-index/src/parse/entities.ts`. Reassessment and implementation proved the bounded seam was already fully characterized by the existing candidate shape, so the truthful fix stayed in `src/parse/stoplist.ts` plus regression tests.
