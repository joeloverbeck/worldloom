# SPEC-01-026: Restrict `ONTOLOGY.md` registry loading to explicit entity entries

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — `tools/world-index` ONTOLOGY registry parsing plus parser/unit and live-corpus regression checks that prove explanatory ONTOLOGY notes are not promoted into canonical entities.
**Deps**: `archive/specs/SPEC-10-entity-surface-redesign.md`, `archive/tickets/SPEC10ENTSUR-001.md`, `archive/tickets/SPEC10ENTSUR-008.md`, `docs/FOUNDATIONS.md`

## Problem

Fresh rebuild audit on `2026-04-23` shows `worlds/animalia/_index/world.db` is structurally clean but still semantically wrong on one Stage A authority seam. After:

1. `cd tools/world-index && npm run build`
2. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
3. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`

the rebuilt DB contains canonical `entities` rows whose `canonical_name` is not an ONTOLOGY registry entity at all, but an explanatory sentence copied from `worlds/animalia/ONTOLOGY.md` `## Notes on Use`, for example:

- `A canon fact may attach to multiple categories (a ward-breach event is both event and hazard; a chartered guild is both institution and faction in some polities).`
- `Mythical-species sentients are species, not artifact, even though M-5 (Mystery Reserve) entertains the heretical possibility otherwise. The category attachment reflects what the world treats as true; mystery entries hold the disquieting alternatives.`
- `The magic practice category is intentionally narrow in Animalia — there is no spellcraft. All entries here are artifact-handling practices.`
- `Artifact-mutated non-sentient beasts attach to hazard + species (non-sentient fauna sub-category) + optionally local_anomaly (for specific named zones) + historical_process (for the centuries-accumulated phenomenon). They do NOT attach to \`person\`, \`faction\`, or any sentient-entity category. They are categorically DISTINCT from Cluster D mythic-species sentient folk (DIS-3) and from Maker-Age guardian constructions (CF-0029; distinct-origin firewall). The surface-similar appearance (chimeric / anomalous morphology) must not collapse the taxonomy.`
- `Mundane-tier Maker-Age artifacts attach to artifact (mundane-subclass) + resource_distribution + hazard (low-tier CAU-1 cost). NOT a new distribution tier — explicit naming of DIS-1 "most inert junk" band. Attach to daily_routine for ordinary-life encounter contexts.`

This is not stale checked-in `_index` residue. It reproduces after fresh rebuild, and the parser path itself proves the mechanism: `loadOntologyRegistry()` scans every `- ` bullet in `ONTOLOGY.md`, while `parseRegistryLine()` falls back to treating the full line as a canonical entity even when no explicit registry syntax is present. That violates the SPEC-10 contract that canonical `entities` must come only from authority-backed registry entries or other structured anchors.

## Assumption Reassessment (2026-04-23)

1. The rebuilt DB is structurally trustworthy. Fresh `animalia` rebuild + `verify` succeeded, `PRAGMA integrity_check` returned `ok`, `validation_results` is empty, unresolved edges are `0`, dangling edge sources/targets are `0`, dangling resolved and unresolved `entity_mentions` are `0`, and `file_versions` covers `59` files.
2. `archive/specs/SPEC-10-entity-surface-redesign.md` defines the Stage A authority surface narrowly: ONTOLOGY-backed canonicals are supposed to come from `loadOntologyRegistry()` registry entries with kind annotations, while heuristic or explanatory prose remains evidence-only. The spec explicitly describes ONTOLOGY-backed canonicals as `registry entries`, not arbitrary bullet prose.
3. The live implementation in `tools/world-index/src/parse/entities.ts` does not currently enforce that contract. `loadOntologyRegistry()` reads every bullet line in `ONTOLOGY.md`, and `parseRegistryLine()` accepts three shapes: `(a)` `Name (kind, ...)`, `(b)` `Name attaches to **kind**`, and `(c)` a bare fallback that returns the entire line as `canonicalName` with `kind: null`.
4. Shared boundary under audit: `tools/world-index/src/parse/entities.ts::loadOntologyRegistry()` and `parseRegistryLine()`, the Stage A canonical `entities` / virtual `named_entity` rows they produce, and the integration verification surfaces in `tools/world-index/tests/integration/build-animalia.test.ts` and `tools/world-index/tests/integration/spec10-verification.sh` that should keep this authority boundary honest.
5. Exact live parser evidence:
   - `node - <<'NODE' ... loadOntologyRegistry('./worlds/animalia/ONTOLOGY.md') ... NODE` currently reports `entry_count = 11`, including multiple full-sentence entries longer than `80` characters from `## Notes on Use`.
   - Those same strings appear in rebuilt SQLite as canonical `entities.canonical_name` rows with `provenance_scope='world'`, `authority_level='structured_anchor'`, `source_node_id = entity_id`, and sentinel `named_entity` source metadata pointing at `ONTOLOGY.md`.
6. Exact source rows in [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:69):
   - [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:71) `- A canon fact may attach to multiple categories...`
   - [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:72) `- Mythical-species sentients are **species**, not **artifact**...`
   - [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:73) `- The **magic practice** category is intentionally narrow in Animalia...`
   - [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:74) `- Artifact-mutated non-sentient beasts... attach to **hazard** + **species** ...`
   - [ONTOLOGY.md](/home/joeloverbeck/projects/worldloom/worlds/animalia/ONTOLOGY.md:76) `- Mundane-tier Maker-Age artifacts... attach to **artifact** ...`
7. `archive/tickets/SPEC10ENTSUR-001.md` truthfully claims that canonical entities now come only from authority-backed sources, but the current parser still allows explanatory ONTOLOGY notes to masquerade as authority-backed sources. That is a live regression against the shipped Stage A contract, not merely another heuristic false-positive seam from the earlier `SPEC-01-020` through `SPEC-01-024` stoplist series.
8. `archive/tickets/SPEC10ENTSUR-008.md`, `tools/world-index/tests/integration/build-animalia.test.ts`, and `tools/world-index/tests/integration/spec10-verification.sh` do not currently assert against this regression class. The capstone passes because its banned list covers earlier false-positive phrases and Mystery Reserve titles, but not malformed ONTOLOGY-note canonicals. That missing proof is a required consequence of the same seam, not a separate audit-only ticket.
9. `docs/FOUNDATIONS.md` `Core Principle` and `Ontology Categories` require canonical entities to reinforce a constrained world model rather than promote explanatory guidance about category use into first-class modeled entities.
10. Mismatch + correction: there is no separate bullet-only ONTOLOGY registry section to gate on in `worlds/animalia/ONTOLOGY.md`; the valid Stage A anchors and the malformed explanatory notes currently coexist inside `## Notes on Use`. The truthful parser boundary is therefore syntax-based, not section-based.
11. Mismatch + correction: the live corpus uses both `attaches to` and `attach to` phrasing in valid authority-bearing bullets. The truthful fix is to remove the bare-line fallback while broadening the explicit matcher to accept the singular/plural verb variants used by the current ONTOLOGY prose.
12. Mismatch + correction: `tools/world-index/package.json` and `tools/world-index/tests/integration/spec10-verification.sh` already exist from `archive/tickets/SPEC10ENTSUR-008.md`. This ticket does not add a new capstone entrypoint; it tightens the existing shell-level banned-canonical coverage.

## Architecture Check

1. The clean fix is to require explicit ONTOLOGY registry syntax for Stage A parsing, instead of relying on a fallback that silently promotes any bullet prose into a canonical entity. That is cleaner than adding another downstream blacklist for sentence-shaped canonicals after they have already crossed the authority boundary.
2. No backwards-compatibility aliasing or shadow canonical layer should be introduced. The canonical `entities` / `named_entity` surface itself must become trustworthy again.

## Verification Layers

1. `loadOntologyRegistry()` no longer returns explanatory `## Notes on Use` bullets as registry entries. -> targeted parser/unit coverage in `tools/world-index/tests/entities.test.ts`
2. Fresh rebuilt `animalia` contains `0` canonical `entities` / `named_entity` rows for the reproduced ONTOLOGY-note sentences. -> readonly DB query in `tools/world-index/tests/integration/build-animalia.test.ts`
3. Legitimate structured anchors still remain canonical after the parser restriction. -> rebuilt-`animalia` retained-anchor assertions plus existing character/artifact checks
4. The SPEC-10 capstone fails if explanatory ONTOLOGY notes re-enter the canonical surface. -> `tools/world-index/tests/integration/spec10-verification.sh`
5. The fix remains semantic-only; rebuild and verify continue to succeed on the live corpus. -> `cd tools/world-index && npm run build`, `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`, `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`

## What to Change

### 1. Narrow the ONTOLOGY registry parser to explicit entity-bearing syntax

Update `tools/world-index/src/parse/entities.ts` so Stage A only accepts explicit ONTOLOGY registry entries rather than every bullet in the file. The truthful boundary is stricter syntax recognition: keep explicit structured shapes, accept the live `attach` / `attaches` verb variants used by real registry bullets, and remove the bare-line fallback so explanatory category-usage notes cannot cross into canonical promotion.

### 2. Add regression coverage for the reproduced ONTOLOGY-note seam

Extend parser/unit tests and rebuilt-`animalia` integration proof so sentence-shaped ONTOLOGY notes are asserted absent from the canonical entity surface.

### 3. Expand the SPEC-10 capstone banned-canonical coverage

Add this regression class to `tools/world-index/tests/integration/spec10-verification.sh` so the shell-level acceptance surface fails if explanatory ONTOLOGY notes are promoted again.

## Files to Touch

- `tools/world-index/src/parse/entities.ts` (modify)
- `tools/world-index/tests/entities.test.ts` (modify)
- `tools/world-index/tests/integration/build-animalia.test.ts` (modify)
- `tools/world-index/tests/integration/spec10-verification.sh` (modify)

## Out of Scope

- Editing `worlds/animalia/ONTOLOGY.md` or any other world canon file to fit the parser
- Reopening the earlier `SPEC-01-020` through `SPEC-01-024` stoplist-only cleanup seam
- Broad redesign of non-ONTOLOGY authority sources (`character_record`, `character_proposal_card`, `diegetic_artifact_record`)
- Adding a generic semantic blacklist for sentence-shaped canonicals outside the ONTOLOGY authority parser

## Acceptance Criteria

### Tests That Must Pass

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd tools/world-index && npm run test:spec10-verification`
5. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
6. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`

### Invariants

1. Stage A ONTOLOGY-backed canonicals come only from explicit authority-bearing registry entries, not from explanatory ONTOLOGY notes about category usage.
2. Downstream consumers do not need a second-layer filter to subtract sentence-shaped ONTOLOGY guidance from the canonical `entities` surface.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/entities.test.ts` — proves `loadOntologyRegistry()` rejects explanatory ONTOLOGY note bullets while keeping explicit registry entries.
2. `tools/world-index/tests/integration/build-animalia.test.ts` — proves rebuilt `animalia` no longer persists the reproduced ONTOLOGY-note canonicals while retained structured anchors still exist.
3. `tools/world-index/tests/integration/spec10-verification.sh` — extends the SPEC-10 capstone banned-canonical set so this regression class cannot hide behind an otherwise-green rebuild.

### Commands

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd tools/world-index && npm run test:spec10-verification`
5. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js build animalia`
6. `cd /home/joeloverbeck/projects/worldloom && node tools/world-index/dist/src/cli.js verify animalia`
7. `cd /home/joeloverbeck/projects/worldloom && node - <<'NODE'
const Database=require('./tools/world-index/node_modules/better-sqlite3');
const db=new Database('./worlds/animalia/_index/world.db',{readonly:true});
const names=[
  'A canon fact may attach to multiple categories (a ward-breach event is both event and hazard; a chartered guild is both institution and faction in some polities).',
  'Mythical-species sentients are species, not artifact, even though M-5 (Mystery Reserve) entertains the heretical possibility otherwise. The category attachment reflects what the world treats as true; mystery entries hold the disquieting alternatives.',
  'The magic practice category is intentionally narrow in Animalia — there is no spellcraft. All entries here are artifact-handling practices.',
  'Artifact-mutated non-sentient beasts attach to hazard + species (non-sentient fauna sub-category) + optionally local_anomaly (for specific named zones) + historical_process (for the centuries-accumulated phenomenon). They do NOT attach to `person`, `faction`, or any sentient-entity category. They are categorically DISTINCT from Cluster D mythic-species sentient folk (DIS-3) and from Maker-Age guardian constructions (CF-0029; distinct-origin firewall). The surface-similar appearance (chimeric / anomalous morphology) must not collapse the taxonomy.',
  'Mundane-tier Maker-Age artifacts attach to artifact (mundane-subclass) + resource_distribution + hazard (low-tier CAU-1 cost). NOT a new distribution tier — explicit naming of DIS-1 \"most inert junk\" band. Attach to daily_routine for ordinary-life encounter contexts.'
];
console.log(JSON.stringify(names.map(name => ({
  name,
  entity_rows: db.prepare('SELECT COUNT(*) AS c FROM entities WHERE world_slug = ? AND canonical_name = ?').get('animalia', name).c,
  named_entity_rows: db.prepare(\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug = ? AND node_type = 'named_entity' AND body LIKE ?\").get('animalia', `Canonical name: ${name} |%`).c
})), null, 2));
db.close();
NODE`

## Outcome

- Completion date: 2026-04-23.
- Removed the ONTOLOGY bare-line fallback from `tools/world-index/src/parse/entities.ts` and broadened the explicit structured matcher from `attaches to` to `attach` / `attaches`, which keeps valid authority-bearing bullets canonical while blocking explanatory note prose from Stage A promotion.
- Added unit coverage proving `loadOntologyRegistry()` rejects explanatory `## Notes on Use` bullets but still keeps explicit `attach(es) to` registry entries.
- Expanded rebuilt-`animalia` integration and SPEC-10 shell-capstone banned-canonical coverage to assert the five reproduced sentence-shaped ONTOLOGY-note canonicals are absent, while `Maker-Age artifact destruction-resistance` remains canonical as a valid structured ONTOLOGY anchor.

## Verification Result

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd tools/world-index && npm run test:spec10-verification`

## Deviations

- The draft allowed for an explicit ONTOLOGY section gate. Live reassessment showed that valid authority-bearing bullets and malformed explanatory notes currently coexist inside `worlds/animalia/ONTOLOGY.md` `## Notes on Use`, so the truthful fix was syntax-only rather than section-based.
- `tools/world-index/package.json` and the SPEC-10 capstone script already existed from `SPEC10ENTSUR-008`; this ticket tightened the existing capstone assertions instead of adding a new entrypoint.
