# SPEC-01-013: Use canonical frontmatter IDs for file-backed record nodes

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — `tools/world-index/src/parse/prose.ts` needs frontmatter-aware node-id selection for whole-file records, with corresponding test and integration updates.
**Deps**: `archive/tickets/SPEC-01-006.md`, `archive/tickets/SPEC-01-008.md`, `docs/FOUNDATIONS.md`, `specs/SPEC-04-validator-framework.md`

## Problem

Whole-file records such as diegetic artifacts, characters, proposals, and audits currently index under slug/path-derived node ids instead of their declared frontmatter ids. In live animalia data, `worlds/animalia/diegetic-artifacts/a-season-on-the-circuit.md` declares `artifact_id: DA-0001`, but the indexed node id is `animalia:a-season-on-the-circuit.md:a-season-on-the-circuit:0`. As a result, `derived_from: [DA-0001]`, `source_artifact_id: DA-0001`, and similar cross-file references cannot resolve through `cross_file_reference`.

## Assumption Reassessment (2026-04-22)

1. `tools/world-index/src/parse/prose.ts` routes subdirectory markdown such as `diegetic-artifacts/*.md`, `characters/*.md`, `proposals/*.md`, `character-proposals/*.md`, and `audits/*.md` through `createWholeFileRecord()`, which currently constructs the node id from file path + heading path and never inspects frontmatter identifier fields.
2. Live animalia files carry canonical ids in frontmatter: `artifact_id: DA-0001` / `DA-0002`, `character_id: CHAR-0001` / `CHAR-0002`, `proposal_id: PR-0010` and `NCP-0001`, and `audit_id: AU-0001`.
3. Live DB evidence shows the only currently unresolved `derived_from` target is `DA-0001` (`3` edges), and the indexed `diegetic_artifact_record` nodes use slug-derived ids rather than `DA-*`.
4. `specs/SPEC-04-validator-framework.md` requires every id referenced in `derived_from`, `required_world_updates`, `affected_fact_ids`, and related fields to resolve to an indexed node. The current whole-file node-id contract violates that requirement for frontmatter-backed record types.
5. Cross-artifact boundary under audit: frontmatter schema for file-backed records, `NodeRow.node_id`, and all graph consumers that expect structured ids (`inspect`, retrieval, validators, proposal/adjudication references).
6. Mismatch + correction: this should be solved generically for file-backed record classes with canonical frontmatter ids, not patched narrowly for `DA-*` only.
7. Mismatch + correction: the existing fixture-world command tests already use ID-prefixed filenames (`DA-0001-sample.md`, `CHAR-0001-sample.md`, `PR-0001-sample.md`, `NCP-0001-sample.md`, `AU-0001-sample.md`), so they do not exercise the animalia-style non-ID filename failure. The truthful proof surface needs a dedicated parser test with non-ID filenames plus an animalia integration assertion; `commands.test.ts` does not need direct changes for this ticket.
8. Mismatch + correction: the drafted CLI proof command assumed `animalia` could be built from `tools/world-index/`, but the CLI resolves worlds relative to `process.cwd()`. The truthful command shape is repo-root scoped (`cd <repo-root> && node tools/world-index/dist/src/cli.js build animalia`). During verification, the live repo’s `worlds/animalia/_index` already contained an unrelated empty-schema DB plus `index_version.txt`, so the end-to-end CLI proof was run against a fresh temp copy of `animalia` with `_index/` removed rather than mutating the user’s live world state.

## Architecture Check

1. The clean solution is to parse just enough frontmatter to select the canonical node id for whole-file record types that already declare one, while leaving body hashing and node typing otherwise unchanged. That preserves the authored identity instead of manufacturing a second one.
2. No backwards-compatibility aliasing should create duplicate rows under both slug ids and canonical ids. The canonical frontmatter id should become the single node id when present.

## Verification Layers

1. A diegetic artifact with `artifact_id: DA-0001` indexes as `node_id='DA-0001'`. -> targeted parser test
2. A character with `character_id: CHAR-0001`, a proposal with `proposal_id: PR-0010`, a character proposal with `proposal_id: NCP-0001`, and an audit with `audit_id: AU-0001` each index under their declared ids even when the filename itself has no structured id. -> targeted parser test
3. Animalia `DA-0001` references resolve after build, and the `a-season-on-the-circuit.md` whole-file record indexes as `node_id='DA-0001'` with no unresolved `derived_from` edges to `DA-0001`. -> integration test + readonly DB query
4. A whole-file record with missing or malformed canonical id falls back to the existing deterministic synthetic/slug path rather than inventing a bad structured id. -> edge-case parser test

## What to Change

### 1. Add frontmatter-aware id selection for whole-file records

Teach `createWholeFileRecord()` to parse the opening YAML frontmatter and prefer the canonical id field for node types that define one: `artifact_id`, `character_id`, `proposal_id`, and `audit_id`.

### 2. Keep the contract generic across file-backed record families

The same mechanism should cover `diegetic-artifacts`, `characters`, `proposals`, `character-proposals`, and `audits`, with explicit tests for each family that currently carries a canonical id field.

### 3. Add live graph regression coverage

Prove that the animalia `DA-0001` seam is fixed and that the new id contract does not create duplicate whole-file nodes.

## Files to Touch

- `tools/world-index/src/parse/prose.ts` (modify)
- `tools/world-index/tests/integration/build-animalia.test.ts` (modify)
- `tools/world-index/tests/prose-whole-file.test.ts` (new)

## Out of Scope

- Extracting new semantic edges from proposal/adjudication frontmatter beyond the node-id fix
- Repairing malformed content in the world corpus
- Solving `required_world_update` target resolution

## Acceptance Criteria

### Tests That Must Pass

1. Whole-file fixture records index under `DA-*`, `CHAR-*`, `PR-*`, `NCP-*`, and `AU-*` when those ids exist in frontmatter.
2. A malformed or absent canonical id field falls back deterministically without creating a duplicate structured-id node.
3. `build animalia` leaves `0` unresolved `derived_from` edges to `DA-0001`, and `diegetic-artifacts/a-season-on-the-circuit.md` indexes as `node_id='DA-0001'`.

### Invariants

1. Whole-file record identity is authored by canonical frontmatter ids when present.
2. The index must never emit two nodes for the same whole-file record solely to preserve the old slug-based identity.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/prose-whole-file.test.ts` — covers frontmatter-id extraction across whole-file record families and malformed-id fallback.
2. `tools/world-index/tests/integration/build-animalia.test.ts` — proves `DA-0001` resolves on the live corpus and that the file-backed artifact row itself now uses `node_id='DA-0001'`.
3. None — existing `commands.test.ts` coverage remains sufficient because its fixture files already use structured ids in filenames; this ticket’s regression is at the parser + live-corpus seam instead.

### Commands

1. `cd tools/world-index && npm run build && node --test dist/tests/prose-whole-file.test.js`
2. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
3. `cd <repo-root> && node tools/world-index/dist/src/cli.js build animalia`
4. `cd <repo-root> && node -e "const Database=require('./tools/world-index/node_modules/better-sqlite3');const db=new Database('worlds/animalia/_index/world.db',{readonly:true});console.log(db.prepare(\`SELECT source_node_id, target_node_id, target_unresolved_ref FROM edges WHERE edge_type='derived_from' AND (target_node_id='DA-0001' OR target_unresolved_ref='DA-0001') ORDER BY source_node_id\`).all())"`

## Outcome

Completion date: 2026-04-22.

`tools/world-index/src/parse/prose.ts` now treats canonical frontmatter ids as the authoritative `node_id` for whole-file record families that already declare one in authored markdown: `artifact_id`, `character_id`, `proposal_id`, and `audit_id`. The parser reads only the opening YAML frontmatter, uses the canonical id when it matches the expected structured-id family, and otherwise falls back to the existing filename/synthetic node-id logic with no alias rows or duplicate identities.

The proof surface was tightened to the real regression seam. A new parser test covers animalia-style non-ID filenames across artifacts, characters, proposals, character proposals, and audits, including malformed or missing-id fallback. The animalia integration suite now asserts that `diegetic-artifacts/a-season-on-the-circuit.md` indexes as `DA-0001` and that all three `derived_from` edges to `DA-0001` resolve with `target_unresolved_ref = null`.

## Verification Result

Completed:

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/prose-whole-file.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `cd /tmp/worldloom-spec01-013-bSkQTK && node /home/joeloverbeck/projects/worldloom/tools/world-index/dist/src/cli.js build animalia`
5. `cd /home/joeloverbeck/projects/worldloom && node -e "const Database=require('/home/joeloverbeck/projects/worldloom/tools/world-index/node_modules/better-sqlite3');const db=new Database('/tmp/worldloom-spec01-013-bSkQTK/worlds/animalia/_index/world.db',{readonly:true});console.log(JSON.stringify(db.prepare(\\"SELECT source_node_id, target_node_id, target_unresolved_ref FROM edges WHERE edge_type='derived_from' AND (target_node_id='DA-0001' OR target_unresolved_ref='DA-0001') ORDER BY source_node_id\\").all()));"`
6. `cd /home/joeloverbeck/projects/worldloom && node -e "const Database=require('/home/joeloverbeck/projects/worldloom/tools/world-index/node_modules/better-sqlite3');const db=new Database('/tmp/worldloom-spec01-013-bSkQTK/worlds/animalia/_index/world.db',{readonly:true});console.log(JSON.stringify(db.prepare(\\"SELECT node_id, file_path FROM nodes WHERE node_type='diegetic_artifact_record' AND file_path='diegetic-artifacts/a-season-on-the-circuit.md'\\").get()));"`

Observed results:

1. TypeScript build passed.
2. `prose-whole-file.test.ts` passed.
3. `build-animalia.test.ts` passed, including the new `DA-0001` identity + resolved-edge assertions.
4. The CLI build on the fresh temp copy succeeded.
5. The readonly DB query on that built temp copy returned exactly three `derived_from` rows targeting `DA-0001`, all resolved with `target_unresolved_ref: null`.
6. The readonly node query on that built temp copy returned `{"node_id":"DA-0001","file_path":"diegetic-artifacts/a-season-on-the-circuit.md"}`.

## Deviations

1. The draft’s `commands.test.ts` edit and fixture-world file changes were unnecessary because those fixtures already encode structured ids in the filenames and therefore cannot reproduce the live regression.
2. The drafted package-local CLI command (`cd tools/world-index && node dist/src/cli.js build animalia`) was not a truthful proof surface because the CLI resolves world paths from `process.cwd()`. The command shape was corrected to repo-root scope.
3. The live repo’s `worlds/animalia/_index` already contained unrelated stale state: `index_version.txt` existed beside an empty-schema `world.db`, which caused the direct live-root CLI build to fail before it reached this ticket’s seam (`no such table: file_versions`). To avoid mutating user-local world state during implementation, the end-to-end CLI proof was completed on a fresh temp copy of `animalia` with `_index/` removed.
