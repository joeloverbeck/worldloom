# SPEC-01-006: Pass 3 world-semantic extraction + entity scanner

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — adds `tools/world-index/src/parse/semantic.ts`, `tools/world-index/src/parse/entities.ts`, and `tools/world-index/src/parse/stoplist.ts`. Pass 3 now emits the full live edge set it owns, including `mentions_entity`, along with `named_entity` virtual nodes and entity mention rows.
**Deps**: `tools/world-index/src/schema/types.ts` (live types including `EdgeType` union), `archive/tickets/SPEC-01-005.md` (Pass 2 YAML-backed node rows)

## Problem

Pass 3 is where "12,111 lines of markdown" becomes "structured graph the retrieval layer queries." Three tasks fold together: (a) scan every node body with the structured-ID regex to produce candidate edges; (b) walk typed YAML objects from Pass 2 to emit edges from specific fields (`source_basis.derived_from`, `modification_history[].change_id`, `affected_fact_ids`, `originating_cf` on CH nodes, `pre_figured_by`); (c) extract attribution-comment edges (`<!-- added by CF-NNNN -->`) from prose nodes under the deterministic "nearest-enclosing structural node" source-node rule; (d) scan for named entities via ONTOLOGY.md registry + capitalized-multiword heuristic. Getting (c) wrong — conflating YAML-edge and attribution-edge semantics — was a HIGH finding in the reassessment; this ticket encodes the corrected post-reassessment separation.

## Assumption Reassessment (2026-04-22)

1. SPEC-01 §Parser pipeline Pass 3 (post-reassessment) now specifies:
   - ID reference regex: `/\b(CF|CH|PA|M|DA|CHAR|PR|BATCH|NCP|NCB|AU|RP)-\d+\b/g`.
   - YAML-field edges keyed from typed CF/CH objects; `originating_cf` is read **only** from the `change_log_entry` node, never from the embedded `modification_history` copies, to avoid duplicate edges.
   - Attribution-comment edges: `patched_by` (prose→CF) and `clarified_by` (prose→CH) — distinct from `modified_by`/`originates_in` (the YAML-derived edges). The attribution comment's verb is informational; the target-id prefix selects the edge type.
   - Source-node rule for attribution comments: "nearest enclosing structural node — `subsection` if one exists between the comment and the nearest `section`, else `section`; for comments inside GFM table cells, the source is the containing `subsection` (or `section` if no subsection). Comments inside fenced code blocks are ignored."
2. `entities.ts` uses `worlds/<slug>/ONTOLOGY.md` as the entity registry (animalia ONTOLOGY.md is 81 lines — verified) plus a capitalized-multiword heuristic. Pass 3 is tunable; false positives are acceptable at Phase 1 since SPEC-04's `world-validate` will report drift.
3. Cross-artifact boundary: the `EdgeRow` emissions flow into `SPEC-01-007` edges-CRUD with `target_unresolved_ref` populated when the target node isn't indexed yet; the sync algorithm (`SPEC-01-008`) re-resolves after full-pass. The live type authority in `tools/world-index/src/schema/types.ts` exposes only the 13 post-reassessment edge types, so the regex scan remains an internal parse aid rather than a persisted generic `mentions_id` edge class. `named_entity` virtual nodes are emitted as `NodeRow` with `node_type='named_entity'` + `node_id='entity:<canonical-slug>'`; their `entity_mentions` backing rows are written by the same pass. Three consumer surfaces (retrieval backlink expansion, validator-rule-12 trace-register coverage, patch engine's anchor localization) depend on this pass's emissions.
4. FOUNDATIONS principle under audit: **Rule 6 (No Silent Retcons)** — the `patched_by` / `clarified_by` disambiguation (enforced at this layer) preserves the Rule-6 audit trail by separating "who modified this fact" (`modified_by`) from "who patched this prose" (`patched_by`). **Rule 7 (Preserve Mystery Deliberately)** — the `firewall_for` edge (MR→CF) is the structural enforcement surface retrieval uses to block MR-resolving queries; this pass emits those edges but does not evaluate them.
5. `pre_figured_by` edge: CF YAML field `pre_figured_by:` — array of DA-NNNN / CHAR-NNNN ids. Currently zero occurrences in animalia (forward-looking per SPEC-01).
6. Mismatch + correction: the ticket draft still described generic persisted `mentions_id` candidate edges, but the live edge contract in `tools/world-index/src/schema/types.ts` has no such variant. Implementation therefore keeps structured-id scanning as an internal extraction helper and persists only the 13 spec-listed edge types plus entity mentions.

## Architecture Check

1. Splitting Pass 3 into `semantic.ts` (ID scan + edge extraction) and `entities.ts` (entity scanner) — rather than one mega-module — isolates the tunable-heuristic surface from the deterministic-regex surface. Entity extraction is expected to evolve post-launch; isolating it makes the tuning surface obvious.
2. The source-node rule for attribution comments is a **deterministic** function of mdast position plus the supplied prose-node spans; it never depends on node-body content. That keeps the parser idempotent and makes debugging "why is this edge sourced here" a matter of reading the rule, not tracing author intent. No backwards-compatibility shims.
3. The canonical-slug generator for `named_entity` ids (e.g., `entity:brinewick`) uses NFC-normalized + lowercased + non-alphanumeric→hyphen transformation, with collision fallback to `entity:<slug>-<short-hash>`. Alternative — using the registry's literal name as the id — was rejected because author-register variation ("the Brinewick", "brinewick") should collapse into one entity.

## Verification Layers

1. A CF YAML block with `modification_history: [{ change_id: CH-0014, originating_cf: CF-0039 }]` produces one `modified_by` edge (source = this CF, target = CH-0014). Reading the `originating_cf` of the embedded modification_history entry does NOT produce an `originates_in` edge here — only the CH node's own `originating_cf` field does (deduplication rule).
2. A domain-file paragraph under subsection `### Brinewick history` containing `<!-- added by CF-0041 -->` produces a `patched_by` edge (source = subsection node id, target = CF-0041) → attribution-edge + source-node rule (subsection-preferred).
3. A GFM table cell under section `## Magical Effects` (no subsection) containing `<!-- added by CF-0039 -->` produces a `patched_by` edge whose source is the section-level node → fallback to `section` when no `subsection` exists.
4. An ONTOLOGY.md entry `- Brinewick (polity, coastal)` plus a paragraph containing `The watchmen of Brinewick reported...` produces one `mentions_entity` edge to `entity:brinewick` plus one mention row per prose occurrence → registry + case-insensitive matching.
5. FOUNDATIONS alignment: the edge-type set produced by this pass is exactly the 13 in SPEC-01 §Edge types (post-reassessment) → grep cross-check `EDGE_TYPES` constant in `tools/world-index/src/schema/types.ts`.

## What to Change

### 1. `tools/world-index/src/parse/semantic.ts`

Exports:
- `extractSemanticEdges(tree: Root, lines: string[], filePath: string, yamlNodes: NodeRow[], proseNodes: NodeRow[]): { edges: EdgeRow[]; parseIssues: ValidationResultRow[] }`.

Algorithm:
1. **Structured-id scan + typed extraction** — reuse the regex `/\b(CF|CH|PA|M|DA|CHAR|PR|BATCH|NCP|NCB|AU|RP)-\d+\b/g` as an internal parser aid for field extraction and unresolved-target diagnostics, but persist only the live 13 edge types from `tools/world-index/src/schema/types.ts`.
2. **YAML-field edge extraction** — for each CF `NodeRow` whose body parsed successfully in Pass 2:
   - `source_basis.derived_from` → `derived_from` edges.
   - `required_world_updates` → `required_world_update` edges (target = synthetic file-path node id; file-path nodes are section nodes in the named domain file, resolved by matching the file path).
   - `modification_history[].change_id` → `modified_by` edges.
   - `pre_figured_by` → `pre_figured_by` edges.
   - `applies_to` → `applies_to` edges (when the CF YAML carries an `applies_to:` MR-id list; open-ended per reassessment).
   - CH `NodeRow` → `affected_fact_ids[]` → `affected_fact` edges; `originating_cf` → `originates_in` edge (source = this CH node).
   - MR `mystery_reserve_entry` node → `firewall_for` edges (parse the MR body for `firewall:` or similar structured markers; if the MR is prose-only, skip — document this limitation in code comment).
3. **Attribution-comment extraction** — for every `html` node in the mdast with position + matching `<!--\s*(added|clarified|modified)\s+by\s+(CF|CH)-(\d+)\s*-->`:
   - Resolve the deepest enclosing prose node span from the supplied `proseNodes`, preferring `subsection`, then `bullet_cluster`, then `section`. This is the **source node id**.
   - Target is `<prefix>-<number>` from the regex; target type selects edge type: CF → `patched_by`, CH → `clarified_by`.
   - Skip attribution comments whose containing mdast ancestor is a `code` node (fenced code block).
4. Emit `ValidationResultRow` warnings for: attribution comment with a prefix that isn't `CF` or `CH` (malformed attribution); attribution comment whose target ID isn't present in the current parsed node set (flag as `unresolved_attribution_target` for later sync-time re-resolution).

### 2. `tools/world-index/src/parse/entities.ts`

Exports:
- `extractEntities(tree: Root, proseNodes: NodeRow[], ontologyRegistry: EntityRegistry): { entityNodes: NodeRow[]; mentions: EntityMentionRow[]; edges: EdgeRow[] }`.
- `loadOntologyRegistry(ontologyPath: string): EntityRegistry` — parses `worlds/<slug>/ONTOLOGY.md` into a case-folded lookup of `{ canonicalName, kind }`.

Algorithm:
1. For each registered entity, case-insensitive substring scan of every prose node body; record matches as `EntityMentionRow` with `entity_name` = canonical form.
2. For each prose node body, apply the capitalized-multiword heuristic: regex `/\b([A-Z][a-z]+(?:\s+[A-Z][a-z]+){1,3})\b/g` — candidate names. Filter against a stop list (common patterns like "The Watchman", "A Certain", proper-noun-styled sentence starts). Residual candidates become `entity:<canonical-slug>` virtual nodes + corresponding mentions.
3. Emit one `mentions_entity` edge per unique `(source node, canonical entity)` pair, while keeping per-occurrence `EntityMentionRow` records for the backing mention table.
4. Virtual `named_entity` NodeRows: one per unique canonical entity, `body` = "Canonical name: ... | Kind: ... | Mentions: N". `content_hash` is computed over the body via `contentHashForProse` (deterministic given the same mentions set).

### 3. Stop list asset

Add `tools/world-index/src/parse/stoplist.ts` containing the initial capitalized-multiword stop list: {"The Maker", "The Makers", "A Certain", "Once Upon", ...}. Start conservative; tune via `world-validate` feedback in SPEC-04.

## Files to Touch

- `tools/world-index/src/parse/semantic.ts` (new)
- `tools/world-index/src/parse/entities.ts` (new)
- `tools/world-index/src/parse/stoplist.ts` (new)
- `tools/world-index/tests/fixtures/fixture-semantic-edges.md` (new)
- `tools/world-index/tests/fixtures/fixture-ontology.md` (new — mini-ONTOLOGY.md-shaped file)
- `tools/world-index/tests/semantic.test.ts` (new)
- `tools/world-index/tests/entities.test.ts` (new)

## Out of Scope

- Writing edges / entity rows to SQLite — covered by SPEC-01-007.
- Resolving `target_unresolved_ref` at incremental-sync time — covered by SPEC-01-008.
- Rule-12 trace-register coverage evaluation — that's SPEC-09's validator; Pass 3 just emits the edges the validator queries.
- Tuning the capitalized-multiword heuristic beyond the initial conservative stop list — tuning happens in SPEC-04 follow-ups once `world-validate` provides feedback.

## Acceptance Criteria

### Tests That Must Pass

1. Fixture with a CH YAML block carrying `originating_cf: CF-0039` produces exactly one `originates_in` edge (source=CH node); the identical `originating_cf` appearing inside an embedded `modification_history[]` entry on a sibling CF node does NOT produce a duplicate.
2. Fixture with `<!-- added by CF-0041 -->` under `### Brinewick history` produces one `patched_by` edge whose `source_node_id` is the subsection node's id.
3. Fixture with `<!-- added by CH-0014 -->` in a table cell under `## Economy` (no subsection) produces one `clarified_by` edge whose source is the section node.
4. Fixture with `<!-- added by CF-0041 -->` inside a fenced code block is NOT emitted as an edge.
5. ONTOLOGY.md-style fixture with entity `Brinewick` produces an `entity:brinewick` virtual node, one `mentions_entity` edge from the prose node, and one mention row per prose occurrence.

### Invariants

1. Every emitted edge's `edge_type` is a member of `EDGE_TYPES` from `tools/world-index/src/schema/types.ts` (grep-proof).
2. `patched_by` edges never target a CH; `clarified_by` edges never target a CF — enforced at emit time via target-prefix check, not trusted from the verb (Rule-6 audit-trail separation).
3. `modified_by` and `originates_in` are emitted only from typed YAML parses — never from attribution comments (grep-proof: attribution-comment branch of `semantic.ts` does not write those two edge types).

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/semantic.test.ts` — fixture-backed typed-edge and source-node assertions, including the malformed-prefix warning case and the fenced-code no-op case.
2. `tools/world-index/tests/entities.test.ts` — ONTOLOGY registry parsing + mention counting + stop-list filtering + `mentions_entity` edge assertions.

### Commands

1. `cd tools/world-index && npm run build && node --test dist/tests/semantic.test.js dist/tests/entities.test.js`
2. `cd tools/world-index && grep -n "patched_by\\|clarified_by\\|modified_by\\|originates_in" src/parse/semantic.ts`
3. `cd tools/world-index && rg -n "entity:|mentions_entity" src/parse/entities.ts`

## Outcome

Completion date: 2026-04-22.

Implemented Pass 3 in three new modules: `src/parse/semantic.ts` for typed YAML-edge extraction plus attribution comments, `src/parse/entities.ts` for ONTOLOGY-backed and heuristic entity discovery, and `src/parse/stoplist.ts` for conservative heuristic suppression. The semantic pass emits the live YAML-derived and attribution-comment edge types while preserving the Rule-6 separation between fact-modification edges and prose attribution edges, ignoring fenced-code comments, and recording malformed or currently unresolved attribution targets as parse issues. The entity pass now loads a lightweight ONTOLOGY registry, counts case-insensitive mentions, synthesizes stable `entity:<slug>` virtual nodes with deterministic bodies, and emits `mentions_entity` edges from prose nodes to those virtual entities.

## Verification Result

1. `cd tools/world-index && npm run build` — passed.
2. `cd tools/world-index && node --test dist/tests/semantic.test.js dist/tests/entities.test.js` — passed.
3. `cd tools/world-index && grep -n "patched_by\\|clarified_by\\|modified_by\\|originates_in" src/parse/semantic.ts` — confirmed the attribution branch emits only `patched_by` / `clarified_by`, while YAML parsing owns `modified_by` / `originates_in`.
4. `cd tools/world-index && rg -n "entity:|mentions_entity" src/parse/entities.ts` — confirmed `named_entity` virtual nodes use the `entity:<canonical-slug>` id contract and that `mentions_entity` emission is present in the entity extractor.
5. `cd tools/world-index && rg -n "mentions_entity|extractEntities\\(|EntityMentionRow|entityNodes|entity_mentions" -S` — confirmed `extractEntities` now emits `mentions_entity` edges alongside `entityNodes` and `EntityMentionRow[]`, and the entities test asserts that edge surface directly.

## Deviations

1. The draft's persisted generic `mentions_id` candidate edge was removed during reassessment because the live edge authority in `src/schema/types.ts` does not define that variant. Structured-id scanning remains internal to extraction and diagnostics, which keeps the implementation truthful to the current schema contract.
2. Post-ticket review briefly reopened the ticket when the `mentions_entity` edge class named by `tools/world-index/src/schema/types.ts` and `specs/SPEC-01-world-index.md` was found missing from the first landing. The missing edge producer and direct test coverage were then added before archival.
