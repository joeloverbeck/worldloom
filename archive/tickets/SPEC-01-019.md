# SPEC-01-019: Restore debuggable broad-lane proof for `build-animalia.test.js`

**Status**: COMPLETED
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: Yes — `tools/world-index` integration-test lane and/or its harness structure.
**Deps**: `archive/tickets/SPEC-01-017.md`, `archive/tickets/SPEC-01-018.md`

## Problem

The broad integration proof lane `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js` no longer serves as a truthful end-to-end acceptance surface. During post-ticket review for `SPEC-01-018`, the exact temp-copy rebuild probe for the owned invariant passed, but the full integration file still exited nonzero with only file-level `ERR_TEST_FAILURE` output and no subtest-local trace. That leaves recent tickets unable to tell whether the failure is real current-ticket fallout or opaque harness noise.

## Assumption Reassessment (2026-04-22)

1. The live integration file at `tools/world-index/tests/integration/build-animalia.test.ts` still contains multiple distinct subtests, including the new `named_entity` false-positive regression assertion added by `SPEC-01-018`.
2. In the current checkout, `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js` exits nonzero with only file-level `ERR_TEST_FAILURE` output, while the direct temp-copy rebuild/query probe recorded in `archive/tickets/SPEC-01-018.md` passes against the same invariant.
3. Shared boundary under audit: the broad `node --test` integration lane, subtest visibility/reporting, and the World Index ticket family’s acceptance-proof contract.
4. `archive/tickets/SPEC-01-009.md`, `archive/tickets/SPEC-01-011.md`, `archive/tickets/SPEC-01-012.md`, `archive/tickets/SPEC-01-014.md`, and `archive/tickets/SPEC-01-016.md` all recorded the broad `build-animalia.test.js` lane as passing, while `archive/tickets/SPEC-01-017.md` and `archive/tickets/SPEC-01-018.md` had to classify it as noisy/non-acceptance proof. The current issue is therefore a live proof-surface regression, not just a one-off ticket authoring mistake.
5. FOUNDATIONS-relevant principle under audit: verification should prove the owned invariant truthfully; a broad lane that fails opaquely weakens traceability and post-ticket handoff even when the underlying implementation is correct.
6. Mismatch + correction: running `node dist/tests/integration/build-animalia.test.js` directly exposed two real failing subtests, not just hidden reporter noise. `loadExpectedNodeCounts()` in `tools/world-index/tests/integration/build-animalia.test.ts` still derived `named_entity` counts from a broader node set than `finalizeEntityState()` uses in `tools/world-index/src/commands/shared.ts`, and `tools/world-index/src/commands/verify.ts` falsely compared persisted virtual `named_entity` rows against file-local parser output. This ticket corrects those two harness/verification mismatches instead of adding alternate acceptance paths.

## Architecture Check

1. The clean fix is to restore an actionable broad integration proof surface or make its failure mode expose the owned subtest clearly, rather than teaching each future ticket to abandon the lane ad hoc.
2. No backwards-compatibility aliasing or duplicate proof paths should be introduced; the repo should keep one truthful broad-lane story plus any justified narrower probes.

## Verification Layers

1. The broad integration lane either passes or surfaces a subtest-local failure that identifies the real seam. -> targeted tool command
2. The `SPEC-01-018` retained proof (`named_entity` regression assertion / equivalent direct probe) remains truthful after the harness adjustment. -> targeted tool command
3. Ticket-family handoff is restored so future `SPEC-01-*` tickets can cite one honest broad integration lane without classifying it as opaque noise. -> manual review

## What to Change

### 1. Reassess the failing integration lane

Determine why `node --test dist/tests/integration/build-animalia.test.js` now exits with only file-level failure output in this checkout and whether the issue is test structure, assertion shape, process behavior, or harness/reporter interaction.

### 2. Restore debuggable broad-lane behavior

Adjust the integration test file and any strictly necessary adjacent harness code so the broad lane is once again usable as a truthful broad proof surface, either by passing or by surfacing the failing subtest/actionable seam.

### 3. Retain the narrower proof seam

Keep the focused `SPEC-01-018` regression assertion or its honest equivalent intact so the broad-lane repair does not regress the newly added `named_entity` proof.

## Files to Touch

- `tools/world-index/tests/integration/build-animalia.test.ts` (modify)
- `tools/world-index/src/commands/shared.ts` (modify)
- `tools/world-index/src/commands/verify.ts` (modify)
- `tools/world-index/tests/integration/*` or adjacent test helpers only if reassessment proves they are part of the same failing seam (modify)
- `tickets/SPEC-01-019.md` (modify)

## Out of Scope

- Further retuning of entity extraction behavior already owned by `archive/tickets/SPEC-01-018.md`
- Schema or parser changes unrelated to the integration-lane failure mode
- Rewriting unrelated archived tickets just because they cited the previously healthy broad lane

## Acceptance Criteria

### Tests That Must Pass

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
3. The focused `SPEC-01-018` broad-lane regression subtest or equivalent direct probe still passes after the broad-lane fix.

### Invariants

1. The broad `build-animalia.test.js` lane must be a truthful, debuggable proof surface rather than an opaque file-level failure.
2. Fixing the harness/proof surface must not silently drop the `named_entity` regression coverage added by `SPEC-01-018`.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/integration/build-animalia.test.ts` — restore or expose actionable broad-lane integration proof for the full `animalia` build.
2. `None — if reassessment shows the fix is strictly test-structure/reporter-visible and does not require additional files, keep the change local to the existing integration test file.`

### Commands

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
3. `cd tools/world-index && node --test --test-name-pattern "build filters audited operational-label false positives from animalia named_entity output" dist/tests/integration/build-animalia.test.js`

## Outcome

- **Completion date**: 2026-04-22
- **What changed**: Aligned the `animalia` integration helper with the live entity-source contract by reusing the shared entity-source node set from `tools/world-index/src/commands/shared.ts`, and updated `tools/world-index/src/commands/verify.ts` to skip persisted virtual `named_entity` rows when comparing file-local parsed output to stored nodes. The broad `build-animalia.test.js` lane now passes again without dropping the `SPEC-01-018` focused regression coverage.

## Verification Result

1. `cd tools/world-index && npm run build` — passed
2. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js` — passed
3. `cd tools/world-index && node --test --test-name-pattern "build filters audited operational-label false positives from animalia named_entity output" dist/tests/integration/build-animalia.test.js` — passed
4. `cd tools/world-index && node --test dist/tests/commands.test.js` — passed
