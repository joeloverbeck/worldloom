# SPEC-01-018: Retune heuristic entity extraction against operational-label false positives

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — `tools/world-index` heuristic entity extraction, stoplist/filtering logic, and live regression coverage.
**Deps**: `archive/specs/SPEC-01-world-index.md`, `archive/tickets/SPEC-01-010-entity-heading-seam.md`, `archive/tickets/SPEC-01-017.md`

## Problem

The production `animalia` index is structurally valid but semantically noisy on the `named_entity` surface. Readonly audit queries on 2026-04-22 showed `614` `named_entity` rows, `608` of which had `Kind: unknown`, with obvious non-entity examples such as `Adds Mystery Reserve`, `All Phase`, `Age Details`, `Action Report`, and `Access Path`. These are being persisted as first-class entity nodes and `mentions_entity` edges, which pollutes the graph that downstream retrieval and audit tooling will consume.

## Assumption Reassessment (2026-04-22)

1. The live heuristic in `tools/world-index/src/parse/entities.ts` still scans persisted prose-node bodies with `CAPITALIZED_MULTIWORD_REGEX` after the heading/body seam fix from `archive/tickets/SPEC-01-010-entity-heading-seam.md`, and those bodies still include structural carriers such as fenced YAML in section spans, frontmatter/title text in whole-file records, and GFM table rows in adjudications.
2. Sample readonly DB probes on 2026-04-22 showed false-positive entity mentions coming from operational labels and artifact titles rather than world entities: `Adds Mystery Reserve` was emitted from the `CANON_LEDGER.md` change-log section body, `All Phase` from an adjudication table row, `Age Details` from repeated open-question references, `Action Report` from a diegetic artifact title, and `Access Path` from subsection-structure text.
3. `archive/specs/SPEC-01-world-index.md` positions `named_entity` nodes as a machine-facing semantic layer backed by `entity_mentions`. Persisting workflow labels and structural phrases as entities weakens that contract.
4. Shared boundary under audit: heuristic candidate collection in `parse/entities.ts`, `entity_mentions`, virtual `named_entity` rows, and the retrieval/validator consumers that will query those surfaces directly from SQLite.
5. FOUNDATIONS principle under audit: the index should reinforce the world's constrained model, not turn operational labels and section furniture into pseudo-canon entities.
6. Adjacent issue classification: `archive/tickets/SPEC-01-017.md` owns provenance normalization for synthetic entity rows. This ticket owns which entities should exist at all.

## Architecture Check

1. The clean approach is to make the heuristic more conservative at candidate-generation time by stripping structural markdown carriers before heuristic matching, then pin the few remaining audited label phrases in the stoplist rather than teaching downstream consumers to subtract a growing bag of obvious false positives.
2. No backwards-compatibility alias path should keep emitting the noisy entities alongside a cleaned set.

## Verification Layers

1. Known false positives (`Adds Mystery Reserve`, `All Phase`, `Age Details`, `Action Report`, `Access Path`) are absent from the rebuilt `named_entity` surface. -> readonly DB query
2. Legitimate entities already covered by registry-backed or strong heuristic evidence remain present after the retune. -> targeted tool command + readonly DB query
3. The overall heuristic output becomes materially less "unknown operational label" heavy on the live `animalia` build. -> readonly DB query/manual review

## What to Change

### 1. Retune heuristic candidate extraction

Tighten `tools/world-index/src/parse/entities.ts` so operational labels, workflow metadata phrases, and structural titles stop becoming persisted entities in common worldloom files.

### 2. Add live regression examples

Encode the audited false positives in fixture/unit coverage and, where appropriate, a live `animalia` integration assertion so the noisy cases cannot regress silently.

### 3. Preserve real entity recall

Prove that the retune still keeps legitimate multiword entities that matter to world retrieval instead of solving the problem by effectively disabling heuristic extraction.

## Files to Touch

- `tools/world-index/src/parse/entities.ts` (modify)
- `tools/world-index/src/parse/stoplist.ts` (modify)
- `tools/world-index/tests/entities.test.ts` (modify)
- `tools/world-index/tests/integration/build-animalia.test.ts` (modify)

## Out of Scope

- Changing canon/world files to accommodate the extractor
- Embeddings or ranking work outside SPEC-01
- Synthetic node provenance normalization beyond what is needed to keep the test surface coherent

## Acceptance Criteria

### Tests That Must Pass

1. A rebuilt `animalia` index contains `0` `named_entity` rows for `Adds Mystery Reserve`, `All Phase`, `Age Details`, `Action Report`, and `Access Path`.
2. Existing and new entity extraction tests still prove legitimate heuristic entities such as `Atreia Selviss` and `Bent Willow` are emitted where expected.
3. The rebuilt `animalia` index shows a materially reduced false-positive `Kind: unknown` surface (`481` unknown rows / `487` total named entities in the verified temp-copy rebuild) without suppressing the retained heuristic examples checked above.

### Invariants

1. `named_entity` rows should represent world entities or deliberate retrieval targets, not workflow labels or structural phrases.
2. The heuristic path remains conservative and additive; downstream consumers should not need a second noise-filtering layer to recover the intended entity graph.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/entities.test.ts` — adds structural-markdown false-positive coverage alongside retained free-prose heuristic names.
2. `tools/world-index/tests/integration/build-animalia.test.ts` — adds the audited noisy-name regression assertion and retained-entity check at the live `animalia` build seam.

### Commands

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node -e "const assert=require('node:assert/strict'); const {cpSync,existsSync,mkdtempSync,rmSync}=require('node:fs'); const os=require('node:os'); const path=require('node:path'); const Database=require('better-sqlite3'); const {build}=require('./dist/src/commands/build.js'); const WORLD_SLUG='animalia'; const root=mkdtempSync(path.join(os.tmpdir(),'world-index-spec-01-018-')); try { const src=path.resolve('..','..','worlds',WORLD_SLUG); const target=path.join(root,'worlds',WORLD_SLUG); cpSync(src,target,{recursive:true}); const copiedIndex=path.join(target,'_index'); if (existsSync(copiedIndex)) rmSync(copiedIndex,{recursive:true,force:true}); assert.equal(build(root,WORLD_SLUG),0); const db=new Database(path.join(target,'_index','world.db'),{readonly:true}); try { const bannedRows=db.prepare(\\\"SELECT body FROM nodes WHERE world_slug = ? AND node_type = 'named_entity' AND body IN ('Canonical name: Adds Mystery Reserve | Kind: unknown | Mentions: 2','Canonical name: All Phase | Kind: unknown | Mentions: 1','Canonical name: Age Details | Kind: unknown | Mentions: 15','Canonical name: Action Report | Kind: unknown | Mentions: 2','Canonical name: Access Path | Kind: unknown | Mentions: 1')\\\").all(WORLD_SLUG); const retainedRows=db.prepare(\\\"SELECT body FROM nodes WHERE world_slug = ? AND node_type = 'named_entity' AND body IN ('Canonical name: Atreia Selviss | Kind: unknown | Mentions: 2','Canonical name: Bent Willow | Kind: unknown | Mentions: 3') ORDER BY body\\\").all(WORLD_SLUG); const unknownCount=db.prepare(\\\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug=? AND node_type='named_entity' AND body LIKE ?\\\").get(WORLD_SLUG,'%Kind: unknown%').c; const totalCount=db.prepare(\\\"SELECT COUNT(*) AS c FROM nodes WHERE world_slug=? AND node_type='named_entity'\\\").get(WORLD_SLUG).c; assert.deepEqual(bannedRows,[]); assert.deepEqual(retainedRows,[{ body: 'Canonical name: Atreia Selviss | Kind: unknown | Mentions: 2' },{ body: 'Canonical name: Bent Willow | Kind: unknown | Mentions: 3' }]); console.log(JSON.stringify({unknownCount,totalCount})); } finally { db.close(); } } finally { rmSync(root,{recursive:true,force:true}); }"`
4. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js` — broad integration lane later restored by `archive/tickets/SPEC-01-019.md`; this ticket's acceptance proof remains the temp-copy rebuild/query assertion recorded below.

## Outcome

- **Outcome amended**: 2026-04-22
- **Completion date**: 2026-04-22
- **What changed**: Retuned heuristic candidate extraction so the capitalized-multiword pass ignores structural markdown carriers before scanning, then stoplisted the remaining audited operational-label phrases (`Access Path`, `Action Report`, `Age Details`). Added fixture coverage for frontmatter/heading/table noise and a live `animalia` regression assertion that checks both the audited absences and retained heuristic entities.

## Verification Result

1. `cd tools/world-index && npm run build` — passed
2. `cd tools/world-index && node --test dist/tests/entities.test.js` — passed
3. `cd tools/world-index && node -e "<temp-copy animalia rebuild + audited named_entity probe>"` — passed with `{"unknownCount":481,"totalCount":487}` and with the audited five rows absent while `Atreia Selviss` / `Bent Willow` remained present

## Deviations

- At original closeout, `cd tools/world-index && node --test --test-name-pattern "build filters audited operational-label false positives from animalia named_entity output" dist/tests/integration/build-animalia.test.js` and the broader `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js` lane still exited nonzero without surfacing a subtest-local assertion trace. `archive/tickets/SPEC-01-019.md` later restored those broad-lane surfaces; this ticket's own acceptance proof remains the direct rebuilt-DB assertion recorded above.
