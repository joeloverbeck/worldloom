# Triage — Story-Related Improvements (Sixth Iteration)

**Date**: 2026-05-15
**Source**: `reports/story-related-improvements-sixth-iteration.md` — external review (ChatGPT-Pro) of the post-SPEC-31 branching-story system, fed `docs/FOUNDATIONS.md` + `docs/CONTEXT-PACKET-CONTRACT.md` + `docs/HARD-GATE-DISCIPLINE.md` + `docs/MACHINE-FACING-LAYER.md` + the shared story-state contract + the seven story-pipeline SKILL.md files. 8 evaluated findings (1 P0, 4 P1, 3 P2) + 5 proposed amendments (A1–A5) + 28-item validator/test plan + anti-recommendations list.
**Outcome**: 6 accepted → `specs/SPEC-32-story-contract-hardening-iv.md` (D1–D6); 2 refuted by codebase verification (F1, F8 — both upload-limitation artifacts); 28-item validator/test plan deferred (4 fixtures pulled into SPEC-32; the remaining 24 deferred to a follow-up testing-hardening spec); anti-recommendations §10 confirms-existing-position (no action).
**Verification**: every finding re-checked against the working tree across five parallel Explore agents (one per claim cluster: F2 `denial_patterns`; F3 turn-cycle seed wording + MCP server warning; F4 closeout retrieval contradiction + FOUNDATIONS §Tooling; F5 page-plan hash write order in bootstrap + turn-cycle + contract + compute-pg-hashes.ts; F1/F6/F7 code-presence inventory + eight-gates scope + integration-debt-note inventory).
**Cross-iteration check**: F3 is a follow-on to SPEC-31 D14 — D14 landed the MCP-server-side `story_local_seed_nodes_ignored` warning (verified at `tools/world-mcp/src/tools/get-context-packet.ts:29-40`) and the contract-level documentation, but the skill prose at `branching-story-turn-cycle/SKILL.md:147` is still ambiguous. F4 is a residue of SPEC-31 D10 — D10 converted the closeout Pre-flight wording to MCP retrieval (verified at `:48,:154`), but the FOUNDATIONS Alignment row at `:357` was not updated to match. No overlap with SPEC-28, SPEC-30, or SPEC-31's other deliverables.
**Severity reclassifications**: none. The audit's P0/P1/P2 calls hold up where the findings hold up; F1 (P0) is refuted in its entirety rather than reclassified, and F8 (P2) is refuted likewise.
**Framing corrections (verification headline)**: ChatGPT-Pro's corpus was limited to docs + SKILL.md files; no code/schemas/validators/tests were uploadable. Two findings (F1 "implementation evidence gap, P0"; F8 `prose-quality-instructions.md` missing) were framed as gating issues but are upload-limitation artifacts, not real gaps. Codebase verification confirms `tools/validators` (45 test files), `tools/patch-engine` (26 test files + all required story-record ops at `tools/patch-engine/src/ops/create-story-record.ts`), `tools/world-mcp` (70 test files + all four expected MCP tools), and `reports/prose-quality-instructions.md` all exist. The audit's executive verdict ("the design is strong; the remaining risk is implementation verification and a few exact wording/retrieval fixes") survives — but the implementation-verification piece is largely already done.

## Accepted → SPEC-32 (D1–D6)

| Intake | Severity | Deliverable | One-line rationale |
|---|---|---|---|
| F2 / A1 | P1 | D1 | Real un-implementable check: `branching-story-prose-attach/SKILL.md:185` references `denial_patterns` (Mystery Reserve schema field), but no such field exists in `tools/validators/src/schemas/mystery-reserve.schema.json`, `tools/world-mcp/src/tools/get-firewall-content.ts` (FirewallContent interface lines 14-20), existing M records, or anywhere outside the skill and the archived brainstorm. Replace with firewall-field-derived patterns (`disallowed_cheap_answers[]` + `unknowns[]` + plan §11 `forbidden_resolutions[]`); do not add a schema field per FOUNDATIONS Rule 5. |
| F3 / A2 | P1 | D2 | Real ambiguity: `branching-story-turn-cycle/SKILL.md:147` says `seed_nodes=<active cast + active location + parent's unresolved mystery claims>`; "active cast/location" can mean `STENT`/`STLOC` (story-local), which the context-packet contract at `docs/CONTEXT-PACKET-CONTRACT.md:157` forbids as `seed_nodes` for story-pipeline tasks. SPEC-31 D14 added the MCP-server `story_local_seed_nodes_ignored` warning; this fixes the skill-prose discipline that should pre-resolve to world-scope IDs. |
| F4 / A3 | P1 | D3 | Real residue: `story-promotion-closeout/SKILL.md:48,:154` correctly uses MCP retrieval (per SPEC-31 D10), but the FOUNDATIONS Alignment row at `:357` still says "direct file reads (CF / CH / PA paths)" — documentation loophole authorizing filesystem reads of `_source/canon/`. FOUNDATIONS `:528` requires MCP retrieval for story-pipeline skills. Closeout is the defense against fake canon-addition outputs; high-leverage fix. |
| F5 / A4 | P1 | D4 | Real integrity gap: `branching-story-bootstrap/SKILL.md:370` and `branching-story-turn-cycle/SKILL.md:460` write `pages-prose-plans/PG-<integer>.md` directly after the patch plan accepts the PG record (which committed `plan_hash` over those future bytes), then update bundle `INDEX.md` without re-hashing. Drift between PG commit and INDEX update is caught only when prose-attach is later invoked. `compute-pg-hashes.ts` has no `--verify` mode. Add post-write re-hash step using existing CLI. |
| F6 / A5 | P2 | D5 | Real wording ambiguity (accept-with-modification — sharpening only, no behavioral change): `_shared-templates/story-state-contract.md` §7 opens "every state-changing skill validates against these eight gates at page-plan commit" — phrasing scopes via "at page-plan commit" but leading "every state-changing skill" can mislead non-PG implementers (prose-attach, health-audit, promotion, closeout). Sharpen contract §7 + FOUNDATIONS Rule 7 firewall paragraph to enumerate PG-authoring vs non-PG explicitly. |
| F7 | P2 | D6 | Real cross-skill drift (accept-with-modification — per-ID reconciliation, not blanket sweep): 4 of 7 skills (health-audit, commitment-block-authoring, story-fact-promotion-to-canon, story-promotion-closeout) reference MCPENH-040, MCPENH-041, PEENH-007, PEENH-008, VALENH-011. closeout marks PEENH-007 "Now landed" while sibling skills still flag it as open. Per-ticket-ID adjudication produces consistent landed/open/superseded status across the four skills. |

## Refuted (no action)

| Intake | Severity | Reason |
|---|---|---|
| F1 | P0 | Audit framed "code/schemas/validators/patch-engine/MCP/tests not uploaded" as P0 production blocker. Codebase verification (parallel Explore agent on `tools/`): `tools/validators` ships 45 test files including framework, rules, schemas, structural, CLI integration; `tools/patch-engine` ships 26 test files plus all required story-record ops (`create_pg_record`, `create_se_record`, `create_bel_record`, `create_ststat_record`, `create_chc_record`, `create_slt_record`, `append_story_diegetic_artifact_record`, `create_stent_record`, `create_stloc_record`, `create_stint_record`, `create_obl_record`, `create_cnsq_record`, `create_thr_record`, `create_srel_record`, `create_stobj_record`, `create_sf_record`, `create_br_record`) at `tools/patch-engine/src/ops/create-story-record.ts`; `tools/world-mcp` ships 70 test files plus all four expected tools (`get_firewall_content`, `get_context_packet`, `get_record`, `get_records`) registered in `tools/world-mcp/src/tool-names.ts`. Hooks (`tools/hooks`) ship 5 test files. Total: 166 test files across the machine-facing layer. Upload-limitation artifact; no spec action. |
| F8 | P2 | `reports/prose-quality-instructions.md` exists at the exact path the page-plan templates reference. Upload-limitation artifact; no spec action. |

## Deferred (no action this spec)

| Intake | Reason |
|---|---|
| Section 8 — 28-item validator/test plan | SPEC-32 includes 4 fixture-driven tests directly tied to D1 (×2), D3 (optional, contingent on SPEC-31 D10 coverage), and D4 (×2). The remaining ~24 tests (multi-branch replay, sibling-branch contradictions, mystery accretion chains, social-propagation fixtures, observer-firewall fixtures, branch-isolation fixtures, choice-set non-collapse, terminal proof, saliency starvation, cross-story contradictions, motivation grounding, canon-baseline CH-window, observer firewall hidden knowledge, etc.) are verification-hardening rather than amendment-driven and are deferred to a follow-up testing-hardening spec. None of them blocks the six accepted amendments. |

## Confirms-existing-position (no action)

| Intake | Reason |
|---|---|
| §10 — Anti-recommendations | Each item (dramatic act structure; midpoint/climax/refusal-of-call tracking; global drama manager; word-count targets; prose-as-state; overbroad LLM narrator memory; automatic branch reconvergence; global rumor graph; schema bloat; widening story-local truth into canon; bypassing HARD-GATE / patch engine; adding `denial_patterns` to the schema) confirms an established FOUNDATIONS rejection or this spec's D1 rejection of the field-addition path. The audit itself classifies them as reject. No verdict-flip; the report's anti-recommendations align with current canonical positions. |

## Out-of-report findings

None substantive. Verification was comprehensive across the five claim clusters; no pre-existing contract drift beyond the eight findings was surfaced.

## Implementation-order file

`specs/IMPLEMENTATION-ORDER.md` is absent. Per the brainstorm skill's Step 5 default rule, no index update — single-spec deliverable doesn't qualify for fresh creation (requires ≥3 specs in a logical bundle with meaningful sequencing). SPEC-32's §Verification provides a per-deliverable implementation order within the spec.

## Verification reference

Verification approach: five parallel Explore agents, one per finding cluster.

- **F2 (`denial_patterns`)**: prose-attach SKILL.md `:185`; schema `tools/validators/src/schemas/mystery-reserve.schema.json:7`; MCP `tools/world-mcp/src/tools/get-firewall-content.ts:14-20`; existing M record `worlds/animalia/_source/mystery-reserve/M-1.yaml`; repo-wide grep returns matches only in skill + archived brainstorm + this audit report.
- **F3 (turn-cycle seed wording)**: turn-cycle SKILL.md `:147`; contract `docs/CONTEXT-PACKET-CONTRACT.md:157`; MCP server `tools/world-mcp/src/tools/get-context-packet.ts:29-40` (regex `STORY_LOCAL_SEED_NODE_PATTERN` enumerates all story-local prefixes; `STORY_LOCAL_SEED_NODE_WARNING = "story_local_seed_nodes_ignored"`); SPEC-31 D14 landed the server warning and contract docs.
- **F4 (closeout retrieval contradiction)**: closeout SKILL.md `:48` (HARD-GATE list — MCP retrieval), `:154` (Pre-flight step 5 — `mcp__worldloom__get_records` + `mcp__worldloom__get_record`), `:357` (FOUNDATIONS Alignment Tooling Recommendation row — "direct file reads"); FOUNDATIONS.md `:528` (Story-pipeline skills depend on MCP retrieval surface for world-canon reads); SPEC-31 D10 converted Pre-flight but missed the FOUNDATIONS Alignment row.
- **F5 (page-plan hash post-write gap)**: contract `_shared-templates/story-state-contract.md` §10 write order (steps 1-6, no 5a); bootstrap Phase 10 `:370`; turn-cycle Phase 10 `:460`; compute-pg-hashes CLI at `tools/world-mcp/src/cli/compute-pg-hashes.ts` (no `--verify` mode); prose-attach hash-integrity check at `branching-story-prose-attach/SKILL.md:157-166` (reactive backstop only, runs after INDEX update).
- **F1/F6/F7 (implementation-evidence + eight-gates + integration-debt)**: codebase inventory (`tools/validators/src` 9 subdirs, 45 test files; `tools/patch-engine/src/ops/` 19 files including `create-story-record.ts`; `tools/world-mcp/src/tool-names.ts` registering all four expected tools); contract `_shared-templates/story-state-contract.md` §7 "every state-changing skill validates against these eight gates at page-plan commit"; integration-debt references in health-audit (4 IDs), commitment-block-authoring (1 ID), story-fact-promotion-to-canon (4 IDs), story-promotion-closeout (5 IDs with PEENH-007 marked "Now landed").

Cross-spec context: SPEC-28 (D1–D7, merged 2026-05-13) hardened input legality / belief predicate / promotion source mapping / closeout supersession. SPEC-30 (D1–D10, merged 2026-05-15) hardened PG-1 input legality / branch_path doc / belief predicate split / promotion source widening / mystery evidence_records / commitment effects-or-likely-effects / choice-set non-collapse / motivation grounding / selection rationale / structured SREL direction. SPEC-31 (D1–D14, merged 2026-05-15) hardened lifecycle / retrieval / shape consistency including D10 (closeout Pre-flight → MCP retrieval) and D14 (CONTEXT-PACKET-CONTRACT + MACHINE-FACING-LAYER + MCP server warning for story-local seed nodes). SPEC-32 (this spec) tightens the remaining wording, retrieval, and integrity edges surfaced by the sixth external iteration.
