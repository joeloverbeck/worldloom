# Triage — Story-Related Improvements (Seventh Iteration)

**Date**: 2026-05-16
**Source**: `reports/story-related-improvements-seventh-iteration.md` — external review (ChatGPT-Pro) of the post-SPEC-32 branching-story system, fed `docs/FOUNDATIONS.md` + `docs/CONTEXT-PACKET-CONTRACT.md` + `docs/HARD-GATE-DISCIPLINE.md` + `docs/MACHINE-FACING-LAYER.md` + the shared story-state contract + the seven story-pipeline SKILL.md files. 11 evaluated findings (6 P1, 5 P2, 0 P0) + 10 proposed amendments (A1–A10) + multi-tier validator/test plan (§11) + research synthesis (§12) + anti-recommendations (§13).
**Outcome**: 9 accepted → `specs/SPEC-33-story-pipeline-seventh-iteration-fixes.md` (D1–D9); 1 already-resolved (P2-010 — SPEC-31 STOCONHAR-009 landed the conditional accretion-policy wording); 1 rejected (P2-008 — TS source path vs dist JS path coexist by design, not drift; low-cost clarification rolled into SPEC-33 D7 as a one-line contract note); test plan §11 partially pulled into SPEC-33 deliverables (1 fixture per accepted finding, ~9 total) with the remainder deferred to a follow-up testing-hardening spec; §12 research synthesis confirms-existing-position; §13 anti-recommendations confirms-existing-position.
**Verification**: every finding re-checked against the working tree across seven parallel Explore agents (one per claim cluster: P1-001 turn-cycle seed derivation + MCP server warning; P1-002 prose-attach `PG.SE.promotion_claims[]` path; P1-003 prose-attach Phase 6 write order vs §10; P1-004 + P1-005 closeout package paths + STSTAT propagation; P1-006 persisted-summary recovery across seven consuming skills; P2-007 through P2-011 contract/skill wording drift; one machine-facing-layer survey covering MCP tools, validators, patch-engine ops, world-index schema, specs status).
**Cross-iteration check**: D5 follows SPEC-32 D2's pattern (skill-prose follow-on to an already-implemented tool surface — `create_ststat_record` exists at `tools/patch-engine/src/envelope/schema.ts:76`). D6 is structurally aligned with SPEC-31 D14's pattern (server-side mechanism landed earlier; consuming-skill discipline is the next layer). D8's wording correction builds on SPEC-32 D1's `forbidden_mystery_resolution` rewrite. No overlap with active deliverables in any other spec.
**Severity reclassifications**: none. The audit's P0/P1/P2 calls hold up where the findings hold up.
**Framing corrections (verification headline)**: ChatGPT-Pro's corpus was limited to docs + SKILL.md files; no code/schemas/validators/tests/runtime were uploadable. Two findings (P2-008 and P2-010) were doc-level extrapolations the codebase has already addressed or that misread tool conventions. The auditor's executive verdict — "architecture sound; remaining work is wording / propagation / persisted-summary recovery; no new fields or new managers" — survives intact and matches what the verified findings imply.

## Accepted → SPEC-33 (D1–D9)

| Intake | Severity | Deliverable | One-line rationale |
|---|---|---|---|
| WL-S7-P1-001 / A1 | P1 | D1 | Real schema mismatch: `branching-story-turn-cycle/SKILL.md:147` names `STENT.bound_ent_id` and `STLOC.governing_section_id`; story-state contract §4.5.1 defines only `bound_char_id`, §4.5.8 defines only `bound_ent`. Skill prose is un-executable as written. MCP server's `story_local_seed_nodes_ignored` warning (per SPEC-31 D14, verified at `tools/world-mcp/src/tools/get-context-packet.ts:29-40`) is a defensive backstop, not a substitute. Skill prose replaced with schema-backed anchors only. |
| WL-S7-P1-002 / A2 | P1 | D2 | Real path bug: `branching-story-prose-attach/SKILL.md:213` references `PG.SE.promotion_claims[]`; PG has no nested SE per story-state contract §4.2. Correct path is `SE.promotion_claims[]` on the event loaded via `PG.input.resolved_event_id`. `accept-with-modification` — auditor's A2 over-corrected line 134 (already correct as `SE.promotion_claims[]`); deliverable limits fix to line 213 only. |
| WL-S7-P1-003 / A3 | P1 | D3 | Real write-order violation: prose-attach Phase 6 writes receipt + INDEX before submitting optional `create_se_record` patch when `emit_attach_event: true`. Story-state contract §10 requires patch submit precedes direct artifacts and INDEX. Atomicity discipline; D3 reorders Phase 6 step 4 with explicit abort-on-patch-failure. |
| WL-S7-P1-004 / A4 | P1 | D4 | Real nesting drift: `story-promotion-closeout/SKILL.md:138,154,~230` references `source_records[]` / `branch_path` as top-level; promotion-to-canon Phase 6 (lines 285–288) nests under `proposal_evidence.{source_records,story_branch,source_kind,contradiction_preference,downstream_impact_report}`. Closeout would fail to find source records or silently mark disposition completeness on an empty set. |
| WL-S7-P1-005 / A5 | P1 | D5 | Real propagation gap: closeout output table (line 122) includes STSTAT supersession; Prerequisites schema list (~137), Phase 2 character_outcome subsection (~181), Phase 3 disposition map, and Phase 5 op list (~297) all omit STSTAT. `create_ststat_record` already exists in the patch engine (verified at `tools/patch-engine/src/envelope/schema.ts:76`); pure skill-prose propagation fix. |
| WL-S7-P1-006 / A6 | P1 | D6 | Real shared-discipline gap: MCP implements `get_persisted_packet_slice` (`tools/world-mcp/src/tools/get-persisted-packet-slice.ts`, registered in `tools/world-mcp/src/server.ts`) and assembler emits `delivery_status: persisted_with_summary` correctly; CONTEXT-PACKET-CONTRACT.md + MACHINE-FACING-LAYER.md document recovery; all seven consuming skills (bootstrap, turn-cycle, prose-attach, health-audit, commitment-block-authoring, story-fact-promotion-to-canon, story-promotion-closeout) document zero recovery wording. New shared template at `.claude/skills/_shared-templates/persisted-packet-recovery.md` cross-referenced from each skill closes the gap (DRY; sibling-template pattern). |
| WL-S7-P2-007 / A7 | P2 | D7 | Real wording-vs-schema drift: story-state contract §7 says gate results are recorded in `PG.validation_trace.gates[]`; PG schema §4.2 uses a flat eight-key mapping. Contract prose corrected; new structural validator `validation-trace-shape-compliance.ts` forecloses the same drift re-emerging. The low-cost contract-level note from rejected P2-008 (TS source path vs dist JS path) is rolled in here as a one-line §4.2a clarification. |
| WL-S7-P2-009 / A9 | P2 | D8 | Real retrieval-claim drift: prose-attach `:114` and FOUNDATIONS Alignment row `~:326` say "No world-canon retrieval needed"; Phase 3 check 3 calls `get_firewall_content` unless plan §11 inlines firewall fields. Wording sharpened to reflect conditional retrieval; also threads in the D6 shared-recovery cross-reference. |
| WL-S7-P2-011 | P2 | D9 | Real provenance gap: skills cite PEENH-007, MCPENH-040, MCPENH-041, PEENH-008, VALENH-011, etc. as "now landed" without linking to verification artifacts (e.g., `story-promotion-closeout/SKILL.md:297` declares PEENH-007 "now landed"; `commitment-block-authoring/SKILL.md:36` references MCPENH-041 likewise). `accept-with-modification` `(pragmatic)` — auditor's A11 proposed adding `describe_capabilities` calls to every skill pre-flight; D9 uses archived-ticket / archived-spec links as the lower-cost audit-trail backstop. Under no-trust-deficit conditions the link form is sufficient; structural softening if a future MCP major version makes silent capability drift more likely. Per-ID adjudication shape borrowed from SPEC-32 D6. |

## Already-resolved (no action)

| Intake | Severity | Reason |
|---|---|---|
| WL-S7-P2-010 / A10 | P2 | SPEC-31 STOCONHAR-009 already converted the health-audit Phase 2e wording to the conditional schema-discovery form the auditor's A10 proposed. The skill reads (verified): "If the M record exposes a validator-backed accretion-policy field (`accretion_policy.max_clues` or equivalent), enforce that policy deterministically." The conditional framing IS the schema-discovery guard. The Mystery Reserve schema at `tools/validators/src/schemas/mystery-reserve.schema.json` has no `accretion_policy` field, so the conditional never fires in current state — exactly what A10 would mandate. No spec action; verification confirms the resolution. |

## Rejected (no action; one-line clarification rolled into SPEC-33 D7)

| Intake | Severity | Reason |
|---|---|---|
| WL-S7-P2-008 / A8 | P2 | Not a drift. `tools/world-mcp/src/cli/compute-pg-hashes.ts` is the TypeScript source; `tools/world-mcp/dist/src/cli/compute-pg-hashes.js` is the compiled output. Story-state contract §4.2a / §10 names the source path (canonical reference for code authors); skills name the dist path (runtime invocation for executors). Both paths coexist by design — the TS source vs compiled-JS-for-runtime distinction is inherent to the tooling pattern across the worldloom codebase. `(structural)` reject reason: the convention is correct, the auditor's read of "drift" reflects unfamiliarity with the TS-to-dist build relationship. Low-cost clarification (spell out both paths explicitly in §4.2a) rolled into SPEC-33 D7 as a one-line note rather than a standalone deliverable. |

## Out-of-report findings (codebase survey)

| Finding | Routing | Reason |
|---|---|---|
| Missing audit-named validators: `branch_isolation`, `observer_firewall`, `lie_promoted_silently`, `canon_baseline_drift` as standalone validators | Deferred to follow-up validator-hardening spec | `(pragmatic)` — each validator is a multi-file undertaking (validator + fixtures + registry update + tests), and adding four would roughly double SPEC-33's blast radius. The closest current coverage is `tools/validators/src/structural/canon-drift-classification-evidence.ts` (partial for canon-baseline-drift). Under stronger production-readiness constraints (e.g., production story-bundle authorization), implementing these is on the critical path; the deferral is cost-driven, not structurally permanent. Flagged in SPEC-33 §Risks & Open Questions. |
| No active `specs/` directory contents pre-SPEC-33; no `specs/IMPLEMENTATION-ORDER.md` | Noted, no action | Most recent IMPLEMENTATION-ORDER is `archive/specs/IMPLEMENTATION-ORDER-2026-05-09.md`. SPEC-33 is the first active spec since SPEC-32 archived. No IMPLEMENTATION-ORDER update required. If a future bundle of specs warrants sequencing, a fresh `specs/IMPLEMENTATION-ORDER.md` can be created then. |

## Deferred (no action this spec)

| Source | Routing | Reason |
|---|---|---|
| Audit §11.2 P1 red-team tests (8 tests) | 9 tests pulled into SPEC-33 D1–D9 as per-deliverable fixture artifacts; remainder deferred | The accepted findings each carry one fixture-driven test as their acceptance proof. The remaining tests from §11.2/§11.3/§11.4/§11.5 (e.g., `social_witness_propagation`, `death_incapacity_reconciliation`, `branch_isolation`, `forbidden_mystery_protection`, `canon_baseline_full_ch_window`, `prose_structural_invention`, `belief_fact_separation_lies_rumors`, the §11.5 research-inspired tests) are validator/test-hardening work that belongs in a dedicated testing-hardening spec along with the four missing audit-named validators. Roughly 25+ fixtures deferred. |
| Audit §12 research synthesis (storylets, quality-based narrative, social simulation, Versu, IPOCL, plot graphs, knowledge graphs, LLM-assisted IF) | `confirms-existing-position` | §12.1–§12.8 each conclude "already implemented in spirit" or "adapt: add tests" — the adaptations are tests, not new fields/managers. The test-hardening work is routed to the testing-hardening spec above; the research-side conclusions confirm the current architecture. §12.9 transfer-filter conditions are FOUNDATIONS-aligned. |
| Audit §13 anti-recommendations (act structure, global drama manager, word-count targets, prose-as-state, broad LLM narrator memory, auto-reconverge, global rumor graph, autonomous NPC simulation, etc.) | `confirms-existing-position` | Every entry matches FOUNDATIONS §Story Bundles §4a / §5a / §5c / §9. No action; the anti-recommendations document existing rejections. |

## Quick-reference: SPEC-33 deliverable shape

- **D1** — turn-cycle seed derivation (skill prose; schema-backed fields only).
- **D2** — prose-attach `PG.SE.promotion_claims[]` path bug (skill prose; one-line replacement at line 213).
- **D3** — prose-attach Phase 6 write order (skill prose; patch-submit-first reordering).
- **D4** — closeout proposal-package nesting (skill prose; `proposal_evidence.*` prefix on five fields).
- **D5** — closeout STSTAT propagation (skill prose; Prerequisites + Phase 2 + Phase 3 + Phase 5 additions).
- **D6** — persisted-summary recovery (new shared template + cross-references in seven consuming skills).
- **D7** — `validation_trace.gates[]` wording (contract prose + new structural validator + dist-JS-path note).
- **D8** — prose-attach "no world-canon retrieval needed" stale (skill prose; condition-on-plan-inlining).
- **D9** — "now landed" integration-debt provenance (skill prose; archived-ticket/spec link substitution).
