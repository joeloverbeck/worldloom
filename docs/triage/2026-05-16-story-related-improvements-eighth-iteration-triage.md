# Triage — Story-Related Improvements (Eighth Iteration)

**Date**: 2026-05-16
**Source**: `reports/story-related-improvements-eighth-iteration.md` — external review (ChatGPT-Pro) with GitHub code-search and file-viewing integration (unlike seventh's docs-only access), fed `docs/FOUNDATIONS.md` + the four shared contract docs (`docs/CONTEXT-PACKET-CONTRACT.md` + `docs/HARD-GATE-DISCIPLINE.md` + `docs/MACHINE-FACING-LAYER.md` + shared story-state contract under `.claude/skills/_shared-templates/`) + the seven story-pipeline SKILL.md files + post-SPEC-34 working-tree state. 9 evaluated findings (4 P1, 5 P2, 0 P0) + 8 proposed amendments (A1–A8 plus an A9-area docs item) + multi-tier validator/test plan (§11) + research synthesis (§12) + anti-recommendations (§13).
**Outcome**: 7 accepted → `specs/SPEC-35-story-pipeline-eighth-iteration-fixes.md` (D1, D2, D3, D6, D7, D8, D9); 2 accept-with-modification with deferral routing to validator-hardening-II → SPEC-35 D4 (rename only; full witness coverage deferred) and SPEC-35 D5 (skill-prose downgrade only; full causal-dependency implementation deferred). §12 research synthesis and §13 anti-recommendations both confirms-existing-position (continuation of seventh-iteration verdict). Test plan §11 partially pulled into SPEC-35 deliverables (one fixture per accepted finding, ~7 total); §11.2 P1 red-team tests and §11.3 P2 production-hardening tests routed to the deferred validator-hardening-II spec alongside D4 + D5 full implementation.
**Verification**: every finding re-checked against the working tree across four parallel Explore agents (validator cluster: F1 / F2 / F4 / F5; MCP cluster: F3 / F6 / F7; fixture + docs cluster: F8 / F9; SPEC-34 scope survey: deliverable enumeration + four-validator presence check + patch-engine story-record op uniformity + Hook 3 coverage). All nine codebase claims CONFIRMED by verbatim file:line evidence.
**Cross-iteration check**: F1 and F2 are immediate post-SPEC-34 implementation drift (SPEC-34 D2 `observer_firewall` and D1 `branch_isolation` respectively); F8's padded-fixture rot in `observer-firewall.test.ts` plausibly masked F2's padded-literal bug from CI detection. F3 predates SPEC-31 D14's `story_local_seed_nodes_ignored` warning surface (the warning was added; the underlying filter was not). F4, F6, F7, F9 are pre-existing drift orthogonal to SPEC-34's scope. F5 is documented-but-never-implemented drift predating SPEC-30 era. No overlap with active deliverables in any other spec; `specs/` was empty at SPEC-35 intake (SPEC-34 archived 2026-05-16).
**Severity reclassifications**: none. The audit's P1/P2 calls hold up across the board where the underlying claims hold up.
**Framing corrections (verification headline)**: ChatGPT-Pro had code-search access this iteration (unlike seventh's docs-only), so the audit's specific file:line claims are sharper and verification is mostly a one-pass confirmation rather than a hypothesis check. The auditor's executive verdict — "architecture sound; remaining work is validators / tests / MCP behavior / capability wording / archive-reference hygiene; no new fields, no new managers" — survives intact.

## Accepted → SPEC-35 (D1, D2, D3, D6, D7, D8, D9)

| Intake | Severity | Deliverable | One-line rationale |
|---|---|---|---|
| F1 / A1 | P1 | D1 | Real validator bug at `tools/validators/src/structural/observer-firewall.ts:138–150`: `selectedChoiceForEvent` resolves via `event.parent_page_id → parent page.input.choice_id`; contract requires resolving the child page via `PG.input.resolved_event_id === SE.id` then validating that child's `input.choice_id`. SPEC-34 D2 immediate drift; the deterministic-subset coverage SPEC-34 D2 promised was built against the wrong page. Pure-validator fix per auditor's A1. |
| F2 / A2 | P1 | D2 | Real validator bug at `tools/validators/src/structural/branch-isolation.ts:160`: `isBundleGenesisRecord` hardcodes `"PG-0001"`; FOUNDATIONS-002 mandates unpadded `PG-1`. SPEC-34 D1 immediate drift; F8's padded-fixture rot plausibly masked the bug. Root-page-aware genesis detection via `BR.root_page_id` (preferred) or `parent_page_id === null && turn_index === 0` (fallback) per auditor's A2. |
| F3 / A3 | P1 | D3 | Real MCP behavior bug at `tools/world-mcp/src/tools/get-context-packet.ts:86–94`: passes unfiltered `args.seed_nodes` into `assembleContextPacket`; warning is computed and appended after assembly at `:97–99`. `tools/world-mcp/src/context-packet/local-authority.ts:152–168` queries every seed ID against the world DB → `node_not_found` for story-local IDs or schema-accident leakage. Test at `get-context-packet.story-pipeline.test.ts:93–114` only asserts warning presence, never asserts exclusion. `accept-with-modification`: D3 partitions seeds and proceeds with empty world-seed sets (legitimate for story-pipeline task types) — rejects auditor's A3 sub-recommendation to raise `invalid_input` (would break `story_prose_attach`, `story_turn_cycle`, `story_health_audit` which rely on `story_slug` + `story_bundle_context`). |
| F6 / A6 | P2 | D6 | Real capability text drift at `tools/world-mcp/src/server.ts:438–441`: describes returning `<CLASS>-0001` (padded); `allocate-next-id.ts:11–61` declares `zeroPad: false` for all classes; actual return is `PG-1` (unpadded) per FOUNDATIONS-002. Pure-text fix + capability snapshot test. |
| F7 / A7 | P2 | D7 | Real schema-discovery wiring gap at `tools/world-mcp/src/tools/describe-envelope-schema.ts:425–426`: `create_bel_record` uses `storyPayloadWithGenericRecord("^BEL-[0-9]+$")`; siblings at `:397–424` use `storyPayloadWithRecord(KEY)`. BEL schema file exists at `tools/validators/src/schemas/story-belief.schema.json` but unregistered in `RECORD_SCHEMA_BY_PAYLOAD_KEY` (`:76–103`). Patch engine itself is correctly wired (`create-story-record.ts` STORY_RECORD_SPECS uniform dispatch verified); the gap is MCP-discovery-only. |
| F8 / A8 | P2 | D8 | Real fixture rot AND scope wider than auditor's: auditor named `story-bundle-fixture.ts` (lines 65, 79, 123, 159, 160) and `observer-firewall.test.ts`; verification surfaced `snapshot-replay-equality.test.ts:346` and `recursive-reference-closure.test.ts:311,331` (all using retired `storylet_realized`). Single-pass refresh across all four files per authoring-time site-enumeration discipline. |
| F9 | P2 | D9 | Real docs hygiene at `docs/FOUNDATIONS.md:537,538,540` citing `archive/specs/SPEC-02/03/05`. Each is reasonably superseded by current docs (`docs/MACHINE-FACING-LAYER.md` / `docs/HARD-GATE-DISCIPLINE.md` / `.claude/settings.json` + `tools/hooks/`). FOUNDATIONS as top-of-stack authority should not lean on archived references. |

## Accept-with-modification → SPEC-35 (D4, D5) — full implementation deferred to validator-hardening-II

| Intake | Severity | Deliverable | One-line rationale + softening label |
|---|---|---|---|
| F4 / A4 | P1 | D4 | Real validator-name overclaim at the former witness-coverage validator path: validator parses `non_propagation:` tag syntax + closed-reason coverage only; does NOT compute direct/indirect witnesses, compare `group=` labels, or check BEL coverage. ChatGPT-Pro offers two paths: rename or implement full coverage. `(pragmatic)` softening: full coverage is structurally cleaner but multi-file new-feature work with material blast radius across health-audit + turn-cycle integration. SPEC-35 D4 renames the surface to `non_propagation_tag_shape` (audit-trail correction); full implementation routes to validator-hardening-II. Under no-pragmatic-cost-constraint conditions, the structurally-cleaner path wins. |
| F5 / A5 | P2 | D5 | Real documented-but-missing drift: zero registry / source hits for `causal_dependency_threat_scan` across `tools/validators/src/{structural,rules}/`; the name appears extensively in skill prose (`branching-story-turn-cycle/SKILL.md`, `branching-story-health-audit/SKILL.md`) and FOUNDATIONS as if it were a registered validator. ChatGPT-Pro prefers implementation ("named subcases are deterministic"). `(pragmatic)` softening: four subcases (choice / affordance dependency clobbered, OBL counterparty unavailable, SLT precondition clobbered) require cross-record dependency walks; combined blast radius roughly doubles SPEC-35's scope. SPEC-35 D5 downgrades skill prose to "judgment-based pre-apply review" with forward-pointer; full implementation routes to validator-hardening-II alongside D4. |

## Confirms-existing-position (no action)

| Intake | Rationale |
|---|---|
| §5 "What should not change" (preserve list — present-causal-state, no act structure, no global drama manager, plan-authority boundary, prose-as-receipt, story-local-vs-world separation, BEL/SF separation, STSTAT-derived entity_status, Mystery Reserve firewall, HARD-GATE / patch-engine discipline, context-packet + targeted retrieval, schema-minimalism, append-only / supersession discipline, audit-only SE no-op replay, repo-current-source-over-archived discipline) | Every entry aligns with FOUNDATIONS and the seventh-iteration triage's preservation list. Documents existing decisions. |
| §12 research synthesis (Riedl/Young IPOCL, KG-assisted IF, Neural Story Planning, Moral Stories, INDCOR) | Conclusion: "do not add new narrative-shape fields; the best transfer is more mechanical validation of causal dependencies, witness/social propagation, motivation grounding, graph closure." Matches seventh-iteration §12 verdict. The "more mechanical validation" implication is exactly what D4 + D5 deferral routes to validator-hardening-II. |
| §13 anti-recommendations (act structure, midpoint/climax fields, global drama manager, word-count targets, prose-as-state, autonomous canon promotion, broad narrator memory, new witness-propagation fields without validators-first, autonomous NPC simulation without HARD-GATE, archive specs as current authority) | Every entry matches FOUNDATIONS §Story Bundles §4a / §5a / §5c / §9 and the current-source-over-archived discipline. Documents existing rejections. |

## Out-of-report findings (codebase survey)

| Finding | Routing | Reason |
|---|---|---|
| F8 scope wider than the auditor named | Folded into SPEC-35 D8 | Auditor named two test files; operator grep sweep surfaced two more (`snapshot-replay-equality.test.ts:346`, `recursive-reference-closure.test.ts:311,331`). Single-pass refresh is cheaper than a follow-up sweep ticket per the brainstorm skill's authoring-time site-enumeration discipline. |
| SPEC-34 D2 Outcome deviation context | Folded into SPEC-35 D1 Problem Statement | SPEC-34 Outcome (`archive/specs/SPEC-34-story-validator-hardening.md:327–340`) documented "D2's schema-valid CHC enforcement landed BEL-focused plus SLT holder-consistency coverage, while the SF access-route branch remains defensive malformed-record coverage." F1's bug is the page-resolution layer beneath whatever access-route coverage exists — the deterministic-subset itself was constructed against the wrong page. Worth flagging in SPEC-35 Problem Statement as context for why the bug landed. |
| Hook 3 coverage of story-bundle `_source/` verified solid | Noted, no action | `tools/hooks/src/hook3-guard-direct-edit.ts:39–40` explicitly blocks both `worlds/<slug>/_source/` and `worlds/<slug>/stories/<slug>/_source/<subdir>/*.yaml`. No drift here; recorded for completeness. |
| Patch-engine story-record op uniformity verified | Noted, no action | `tools/patch-engine/src/ops/create-story-record.ts` STORY_RECORD_SPECS lookup dispatches all 16 story-record ops uniformly (including `create_bel_record` with no special-case wiring). The F7 gap is MCP-side discovery only, not patch-engine-side. Recorded for completeness. |
| `specs/` empty at SPEC-35 intake; no `specs/IMPLEMENTATION-ORDER.md` | Noted, no action | SPEC-34 archived 2026-05-16; SPEC-35 is the first active spec since. Single-spec deliverable → no IMPLEMENTATION-ORDER update required. If a future bundle of specs warrants sequencing, a fresh `specs/IMPLEMENTATION-ORDER.md` can be created then. |

## Deferred (no action this spec)

| Source | Routing | Reason |
|---|---|---|
| F4 full witness-coverage validator implementation (auditor's A4 "better change" path) | Deferred to validator-hardening-II spec | `(pragmatic)` — multi-file new-feature undertaking with material blast radius across health-audit + turn-cycle integration. Combined with F5 deferral below, the natural cluster for the second validator-hardening spec. SPEC-34 precedent: deferred validators ship together. |
| F5 `causal_dependency_threat_scan` four-subcase validator implementation (auditor's A5 "prefer implementing" path) | Deferred to validator-hardening-II spec | `(pragmatic)` — cross-record dependency walks across choice / affordance / OBL / SLT records; combined blast radius roughly doubles SPEC-35's scope. Bundled with F4 full implementation per SPEC-34 precedent. |
| Audit §11.2 P1 red-team tests (10 tests covering death/incapacity reconciliation, impossible-write rejection, prose-receipt non-mutation, audit-only no-op replay, promotion-closeout fake-output rejection, canon-baseline full CH-window, non-propagation-group precision, branch-isolation leak, storylet-alias binding, choice-set noncollapse) | 7 tests pulled into SPEC-35 D1–D8 as per-deliverable fixture artifacts; remainder routed to validator-hardening-II | The accepted findings each carry one fixture-driven test as acceptance proof. The remaining tests (notably death/incapacity reconciliation, audit-only no-op replay, canon-baseline full CH-window) become directly executable against new validators / validator-extensions in validator-hardening-II. |
| Audit §11.3 P2 production-hardening tests (6 tests) | Routed to validator-hardening-II | `describe_capabilities_allocator_unpadded_wording` lands as SPEC-35 D6 acceptance; remainder (fixture-rot guard, archive-reference hygiene guard, persisted-summary recovery, post-write plan-hash mismatch, dist-currency rebuild check) are production-hardening discipline that pairs with the full validator-hardening cluster. |
| Audit §11.4 optional research-inspired tests (motivation grounding, social-norm violation propagation, KG reference closure) | `confirms-existing-position` (deferred under existing scope) | §11.4 maps directly onto §12.1–§12.5 research synthesis conclusions ("adapt: add tests"); the adaptations land alongside validator-hardening-II rather than this spec. |

## Quick-reference: SPEC-35 deliverable shape

- **D1** — `observer_firewall` current-choice page resolution (validator + fixture + test; SPEC-34 D2 immediate drift fix).
- **D2** — `branch_isolation` root-page-aware genesis detection (validator + fixture + test; SPEC-34 D1 immediate drift fix).
- **D3** — `get_context_packet` story-local seed partitioning (MCP behavior + integration tests).
- **D4** — witness-coverage overclaim renamed to `non_propagation_tag_shape` (validator file rename + registry + skill / contract sweep; full witness coverage deferred to validator-hardening-II).
- **D5** — `causal_dependency_threat_scan` skill-prose downgrade to "judgment-based pre-apply review" + forward-pointer (skill files + contract; full validator implementation deferred to validator-hardening-II).
- **D6** — `allocate_next_id` capability description text + capability snapshot test (MCP text + test).
- **D7** — `create_bel_record` envelope schema wiring (MCP `RECORD_SCHEMA_BY_PAYLOAD_KEY` + dispatch + schema-discovery test).
- **D8** — Fixture refresh sweep across four test files (`story-bundle-fixture.ts`, `observer-firewall.test.ts`, `snapshot-replay-equality.test.ts`, `recursive-reference-closure.test.ts`) for retired schema fields and padded IDs.
- **D9** — FOUNDATIONS archive-spec reference cleanup (replace three `archive/specs/SPEC-NN-*.md` citations with current-doc paths).
