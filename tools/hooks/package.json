{
  "name": "@worldloom/hooks",
  "version": "0.1.0",
  "type": "module",
  "description": "Claude Code hook scripts for worldloom retrieval discipline",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:dist-currency": "bash scripts/check-dist-currency.sh",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "better-sqlite3": "13.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.13",
    "@types/node": "25.9.1",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
