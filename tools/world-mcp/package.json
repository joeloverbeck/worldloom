{
  "name": "@worldloom/world-mcp",
  "version": "0.1.0",
  "description": "MCP retrieval server scaffold for worldloom machine-facing reads",
  "private": true,
  "bin": {
    "sign-approval-token": "dist/src/cli/sign-approval-token.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/src/cli/sign-approval-token.js",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@worldloom/patch-engine": "file:../patch-engine",
    "@worldloom/validators": "file:../validators",
    "@worldloom/world-index": "file:../world-index",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "12.2.0",
    "yaml": "2.8.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.13",
    "@types/node": "24.7.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
