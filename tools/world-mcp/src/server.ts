// Placeholder entrypoint so the scaffold produces a real build artifact before
// retrieval tools land in subsequent SPEC-02 tickets.
throw new Error(
  "world-mcp scaffold only; retrieval tool implementation lands in subsequent SPEC-02 tickets."
);
