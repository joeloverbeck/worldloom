# SPEC-01-017: Normalize virtual `named_entity` node provenance to the node schema contract

**Status**: PENDING
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: Yes — `tools/world-index` synthetic entity-node construction plus tests/audit coverage.
**Deps**: `archive/specs/SPEC-01-world-index.md`, `archive/tickets/SPEC-01-010-entity-heading-seam.md`

## Problem

The production `animalia` index currently writes malformed provenance metadata for every synthetic `named_entity` node. All `614` `named_entity` rows use an absolute `file_path` (`/home/.../worlds/animalia/ONTOLOGY.md`) while the rest of the index uses world-relative paths, and those same rows store `line_start = line_end = byte_start = byte_end = 0`. That breaks the database's own normalization pattern and makes generic source-range audits flag every synthetic entity row as invalid.

## Assumption Reassessment (2026-04-22)

1. `archive/specs/SPEC-01-world-index.md` defines `nodes.file_path`, `byte_start`, `byte_end`, `line_start`, and `line_end` as part of the public node schema consumed directly from `world.db`.
2. The live constructor in `tools/world-index/src/parse/entities.ts` sets synthetic entity nodes to `file_path: ontologyRegistry.sourcePath` and all location fields to `0`, while normal parsed nodes use relative file paths and 1-based line spans.
3. Readonly audit queries against `worlds/animalia/_index/world.db` on 2026-04-22 showed `614` rows with impossible ranges, and every one of them was a `named_entity` row with an absolute path under the local checkout.
4. Shared boundary under audit: `nodes` table schema, synthetic `named_entity` rows, and downstream consumers that join or filter on `file_path` and source spans.
5. Mismatch + correction: synthetic rows do not need fake provenance, but they do need schema-consistent provenance. The live implementation currently encodes "virtual node" as malformed source metadata instead of as a valid, documented convention.

## Architecture Check

1. The clean solution is to give virtual nodes a consistent, explicit provenance convention that still satisfies the `nodes` schema contract, rather than expecting consumers to special-case absolute paths and zero spans.
2. No backwards-compatibility aliasing should preserve both malformed and normalized provenance shapes.

## Verification Layers

1. All `named_entity` rows use the same world-relative `file_path` convention as the rest of the index. -> readonly DB query
2. No `named_entity` row violates the node location invariant chosen for synthetic nodes. -> readonly DB query
3. Existing entity-node consumers and tests still resolve `mentions_entity` edges correctly after provenance normalization. -> targeted tool command

## What to Change

### 1. Normalize synthetic node provenance

Update synthetic `named_entity` construction so `file_path` is stored in the same relative form as other nodes, and choose a single documented source-location convention for virtual nodes that does not masquerade as malformed parser output.

### 2. Add regression coverage

Add test assertions that synthetic entity nodes do not emit absolute paths or invalid range metadata.

### 3. Preserve consumer clarity

If a special virtual-node convention is needed, document it in the tests/spec-adjacent comments instead of leaving it implicit in malformed values.

## Files to Touch

- `tools/world-index/src/parse/entities.ts` (modify)
- `tools/world-index/tests/entities.test.ts` (modify)
- `tools/world-index/tests/integration/build-animalia.test.ts` and/or `tools/world-index/tests/schema.test.ts` (modify)

## Out of Scope

- Retuning which entity names are extracted
- Changing world markdown content
- Broader schema migration/versioning work outside this provenance seam

## Acceptance Criteria

### Tests That Must Pass

1. A rebuilt `animalia` index contains `0` rows where `node_type = 'named_entity'` and `file_path` is absolute.
2. A rebuilt `animalia` index contains `0` `named_entity` rows that violate the chosen synthetic-node location invariant.
3. Existing entity extraction tests still pass with the normalized provenance shape.

### Invariants

1. `nodes.file_path` remains world-relative across all node types stored in `world.db`.
2. Virtual `named_entity` nodes must not force generic consumers to interpret malformed source locations as a special case.

## Test Plan

### New/Modified Tests

1. `tools/world-index/tests/entities.test.ts` — asserts the synthetic entity-node shape is normalized, not just that ids/mentions exist.
2. `tools/world-index/tests/integration/build-animalia.test.ts` and/or `tools/world-index/tests/schema.test.ts` — proves the live `animalia` build produces no absolute-path `named_entity` rows.

### Commands

1. `cd tools/world-index && npm run build`
2. `cd tools/world-index && node --test dist/tests/entities.test.js`
3. `cd tools/world-index && node --test dist/tests/integration/build-animalia.test.js`
4. `node -e "const Database=require('./tools/world-index/node_modules/better-sqlite3'); const db=new Database('worlds/animalia/_index/world.db',{readonly:true}); console.log(db.prepare(\\\"SELECT COUNT(*) AS c FROM nodes WHERE node_type='named_entity' AND file_path GLOB '/*'\\\").get()); db.close();"`

