name: CI - patch-engine

on:
  push:
    branches: [main]
    paths:
      - 'tools/patch-engine/**'
      - 'tools/world-index/**'
      - 'tests/fixtures/animalia/**'
      - '.github/workflows/ci-patch-engine.yml'
  pull_request:
    paths:
      - 'tools/patch-engine/**'
      - 'tools/world-index/**'
      - 'tests/fixtures/animalia/**'
      - '.github/workflows/ci-patch-engine.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ['22', '24']
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install and build world-index (file dependency)
        working-directory: tools/world-index
        run: npm install && npm run build
      - name: Install
        working-directory: tools/patch-engine
        run: npm install
      - name: Test
        working-directory: tools/patch-engine
        run: npm test
