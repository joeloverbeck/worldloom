name: CI - hooks

on:
  push:
    branches: [main]
    paths:
      - 'tools/hooks/**'
      - 'tools/world-index/**'
      - '.github/workflows/ci-hooks.yml'
  pull_request:
    paths:
      - 'tools/hooks/**'
      - 'tools/world-index/**'
      - '.github/workflows/ci-hooks.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Install
        working-directory: tools/hooks
        run: npm install
      - name: Verify hooks dist currency
        working-directory: tools/hooks
        run: npm run check:dist-currency
      - name: Test
        working-directory: tools/hooks
        run: npm test
